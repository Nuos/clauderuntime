# ClaudeRuntime B4 修复反馈建议 v2

> 文档编号：`CR-B4-REPAIR-FEEDBACK-v2`  
> 文档定位：**针对 B4 遗留问题修复后的定向复核与反馈建议，不是新的总体开发计划。**  
> 当前项目：`Nuos/clauderuntime`  
> 当前 `main` HEAD：`cf4e0f91e6cdcd8105d93585aa9a18fbb1fc11d9`  
> 本轮实际功能修复提交：`4126dd5c7e80f82e542257cf21aef94e3e09e5bf`  
> Claude Code recovered source：`2.1.88`  
> Reference commit：`a8a678cb6244e6770e1e421767ff0987a1d95549`  
> 适用约束：Reference-7 + Reference-5 + CCR-14；`SOURCE_ALIGNED_CORE_IN_PROGRESS`

---

# 0. 总体反馈

这轮针对 B4 遗留问题的优化是**有效且方向正确的第一批修复**。本轮最有价值的变化，不是测试数量增加，而是项目开始主动纠正 B4 阶段过早的 `VERIFIED` 结论，并把一部分“结构存在但行为不对”的实现重新拉回 Claude Code 2.1.88 的源码语义。

本轮可以确认有四类实质进展：

1. **Compact-5 Source-Aligned 主路径修正**
   - 生产默认模式不再因为早层节省 token 而跳过后续 shaping；
   - Microcompact 改为每轮进入，再由内部 gate 决定 no-op；
   - 旧 early-exit 被隔离到显式 `PRODUCT_EXTENSION` 路径。

2. **Context-9 / Path-scoped Rules 从 Permission 误分类改回 Context**
   - Project 路径规则开始进入真实 Read → lazy discovery → meta user context injection 主链；
   - 增加会话级去重、并发锁和 symlink escape 防护。

3. **Machine Evidence 从“完成率优先”转成“事实优先”**
   - 原先不具备完整证据的 `VERIFIED` 被主动降级；
   - 新增 evidence consistency checker；
   - Reference-7 / Reference-5 / CCR-14 成为唯一正式 completion taxonomy。

4. **测试治理更加可信**
   - 本轮项目记录全量本地 Python 测试为 `10151 passed, 10 skipped, 3 warnings, 345 subtests passed`；
   - 文档明确不把本地测试写成 current-HEAD CI green。

但是，本轮还不能写成“B4 遗留问题已完成修复”。当前状态更准确的是：

```text
B4 遗留问题修复：第一阶段有效完成
A0 Truth Reconciliation：部分完成
A1 Compact-5：部分完成
A2 Context-9：部分完成
A3 Durable Resume：未完成
A4 Cross-platform Isolation：未完成
Scheduler / Full Surface / Final CI：未完成
```

---

# 1. 本轮值得确认通过的修复

## 1.1 Compact-5：Source-Aligned 模式不再跨层 early-return

### B4 原问题

原实现允许：

```text
Layer 1 / Layer 2 / Layer 3
→ tokens_saved >= early_exit_tokens
→ return
→ 后续 shaper 不再执行
```

这会改变 Claude Code `query.ts` 中 pre-model context shaping 的整体调用结构。

### 当前修复

当前默认：

```python
source_aligned: bool = True
```

在 Source-Aligned 路径中：

```text
Result Budget
→ Snip
→ Microcompact
→ Context Collapse
→ AutoCompact
```

全部继续访问，由各层自身判断是否 no-op。

旧 early-exit 仅在：

```python
source_aligned=False
```

时保留。

### 反馈结论

**修复接受。**

这是正确的设计边界：

```text
Reference parity path
!=
Product optimization path
```

建议后续继续保持：

- `source_aligned=True` 必须是生产 parity 默认；
- `source_aligned=False` 不得参与 parity score；
- 所有 PRODUCT_EXTENSION 都必须进入 divergence registry。

---

## 1.2 Microcompact：调用位置修正

### B4 原问题

之前由 pipeline 外层直接判断：

```text
mc_enabled == false
→ 整个 Microcompact stage 不进入
```

这和 Reference “调用 stage，再由内部 feature/config 决定 no-op”不同。

### 当前修复

现在每轮进入：

```text
microcompact_typed_messages()
```

默认关闭时通过：

```text
TimeBasedMCConfig(enabled=False)
force=False
```

在 Microcompact 内部返回 no-op。

### 反馈结论

**修复接受。**

这比“外层删掉整个阶段”更接近 Source control-flow。

---

## 1.3 Path-scoped Rules：语义归位正确

### B4 原问题

之前 machine matrix 把：

```text
path_rules
```

归入：

```text
src/permissions
permission_context
```

这是语义错误。

### 当前修复

已经改为：

```text
Read(file)
→ canonical path
→ discover matching project rules
→ claim once
→ inject meta user context
```

并且：

- 不参与 Read 权限判定；
- 规则只作为 context instruction；
- 同一会话只注入一次；
- concurrent Read 通过锁防重复；
- rule symlink 越出项目 root 时拒绝注入。

### 反馈结论

**方向正确，Project/CWD-level 子集修复接受。**

这项修复应保留，不建议再回退到 Permission 层。

---

## 1.4 Machine Evidence 主动降级错误 VERIFIED

### B4 原问题

同一个机制在不同文件中可能出现：

```text
runtime trace = VERIFIED
callgraph = BLOCKED
state map = PARTIAL
scorecard = VERIFIED
```

导致 `VERIFIED` 失去含义。

### 当前修复

现在统一采取更保守的状态：

```text
R7: 6 PARTIAL + 1 BLOCKED
R5: 4 PARTIAL + 1 BLOCKED
CCR: 13 PARTIAL + 1 BLOCKED
```

同时新增：

```text
scripts/audit/check_parity_evidence.py
```

用于检查：

- stale baseline；
- unproven VERIFIED；
- work-item 状态冲突；
- scorecard / ledger mismatch。

### 反馈结论

**修复方向完全正确。**

主动把错误 `VERIFIED` 降级是项目质量提升，不是完成度倒退。

---

## 1.5 Taxonomy 清理

当前已经把正式分类固定为：

```text
Reference-7
Reference-5
CCR-14
```

旧：

```text
5 Parity Layers
AUX-01..14
```

仅作为：

```text
LEGACY_SUPPORTING_MAP
```

不再参与 completion scoring。

### 反馈结论

**基本关闭。**

后续必须避免任何新文档再次把 legacy taxonomy 写成“正式 5 层/14 机制”。

---

# 2. 当前仍未完成的问题

## 2.1 Snip：仍然是 Open Gap

当前 Python：

```python
def snip_compact(...):
    return list(messages), 0
```

因此当前 Layer 2 仍是固定 no-op。

这里需要区分两个问题：

### 问题 A：行为仍未实现

当前不能把 CCR-03 / R5-02 写成 complete。

### 问题 B：当前注释结论过强

当前文件注释写：

```text
The TS implementation is a stub that returns null
```

但 recovered source 目前能确认的是：

```text
query.ts
→ HISTORY_SNIP gate
→ snipCompactIfNeeded(...)
→ tokensFreed / boundaryMessage
```

而不是“Reference Snip 确认就是 stub”。

### 反馈建议

立即调整描述为：

```text
Reference call-site confirmed;
Reference implementation body not yet fully recovered;
Python currently keeps an explicit no-op placeholder;
status = UNKNOWN/PARTIAL.
```

禁止继续用“TS 本身也是 stub”作为完成依据。

### 状态

**OPEN / P0-P1。**

---

## 2.2 Microcompact 默认常量仍存在 Source Drift

当前 Python：

```python
DEFAULT_TIME_BASED_MC_ENABLED = True
```

而 Reference `timeBasedMCConfig.ts` 默认：

```text
enabled: false
gapThresholdMinutes: 60
keepRecent: 5
```

当前生产 pipeline 因为显式覆盖 `enabled=False`，暂时没有导致主路径误触发，但默认值本身仍然不一致。

### 反馈建议

直接改为：

```python
DEFAULT_TIME_BASED_MC_ENABLED = False
```

然后补：

- direct-construction regression；
- main-thread / subagent gating；
- cached microcompact；
- cache_edits；
- keepRecent / gapThreshold source differential。

### 状态

**OPEN / P1。**

---

# 3. Context-9：当前只完成 Project/CWD-level 子集

## 3.1 Managed / User conditional rules 未实现

Reference 有独立：

```text
Managed conditional rules
User conditional rules
Project conditional rules
```

当前 Python 新增的是：

```text
get_project_path_scoped_rules()
```

所以目前只覆盖 Project。

### 建议

补：

```text
get_managed_path_scoped_rules()
get_user_path_scoped_rules()
```

或者设计一个统一：

```text
get_path_scoped_rules(target_path, scope_set)
```

但不要复制三套匹配逻辑。

### 状态

**OPEN。**

---

## 3.2 Nested-directory rules 尚未完成

Reference 支持：

```text
CWD
→ target file path
→ CWD 与 target 之间的 nested directories
→ nested CLAUDE.md / .claude/rules
```

当前 Python 的 root traversal 主要是：

```text
cwd
→ parent
→ parent
```

并不会沿：

```text
cwd
→ target.parent
```

下钻发现 nested rules。

### 示例

```text
repo/
  src/
    service/
      .clawcodex/rules/service.md
      worker.py
```

当 CWD 是 `repo/`，读取 `src/service/worker.py` 时，Reference 可发现 `src/service/.claude/rules`；当前实现并不能完整覆盖该行为。

### 建议

实现：

```text
cwd → target.parent
```

之间的目录链：

```python
relative = target.parent.relative_to(cwd)
for segment in cumulative_segments(relative):
    process nested project memory/rules
```

并继续保持：

- symlink canonicalization；
- target escape rejection；
- rule root provenance；
- dedup；
- conditional/unconditional 分离。

### 状态

**OPEN / 重要。**

---

## 3.3 当前所谓 E2E 仍停留在 Tool 层

当前测试主要证明：

```text
_read_call()
→ ToolResult.new_messages
```

已经带有 path rule。

但 Source-Aligned final proof 应继续到：

```text
Turn 1 model requests Read
→ Read produces result + rule context
→ Query continues
→ Turn 2 provider receives rule
```

### 建议新增

一个 deterministic provider integration test：

```text
model turn 1 → Read(foo.py)
tool result → path rule attachment
model turn 2 → capture request messages
assert rule exactly once
```

并覆盖：

- matching；
- non-matching；
- duplicate read；
- concurrent read；
- compact reload；
- resume reload。

### 状态

**OPEN。**

---

# 4. Machine Evidence：已进步，但 baseline 模型本身仍有设计问题

## 4.1 当前问题

当前仓库 HEAD：

```text
cf4e0f91...
```

而多个 machine evidence 文件仍固定：

```text
baseline_commit: 95efbaec...
```

同时 checker 默认：

```text
expected_baseline = git rev-parse HEAD
```

但是测试又硬编码：

```text
B5_BASELINE = 95efbaec...
```

所以测试可以通过，但并没有证明：

```text
machine evidence == current HEAD
```

---

## 4.2 更根本的问题：不能让一个 commit 内的文件写“自己的 SHA”

如果流程是：

```text
把 HEAD 写入 yaml
→ 修改 yaml
→ commit
→ HEAD 改变
```

那么 YAML 天然立刻 stale。

这是自引用问题。

### 推荐修复模型

采用：

```text
subject_commit
evidence_commit
```

例如：

```yaml
subject_commit: <实际被验收的生产代码提交 X>
reference_commit: <Claude Code reference>
evidence_generated_from: <test run / extractor>
```

然后证据提交 Y 只允许修改：

```text
docs/parity/**
scorecards/**
generated evidence
```

并要求：

```text
git diff X..Y
```

不能出现 production code 修改。

### 更推荐

CI 中生成：

```text
scorecard artifact
```

由：

```text
GITHUB_SHA
```

动态绑定，不要求把同一个 HEAD SHA 回写入源码仓库。

### 状态

**OPEN / A0 P0。**

---

# 5. Evidence Checker：当前还是 Linter，不是 Proof Engine

当前 checker 的主要逻辑仍是：

```text
字段非空
→ 认为 evidence 完整
```

它还没有自动验证：

- reference file 是否真的存在；
- reference symbol 是否存在；
- Python symbol 是否存在；
- call edge 是否真实；
- differential test 是否存在；
- test 是否执行通过；
- safety invariant；
- failure invariant；
- platform evidence；
- divergence 是否阻止 final VERIFIED。

### 建议

把 checker 分成两层：

```text
Layer A — Schema / Consistency Linter
Layer B — Evidence Resolver / Verifier
```

第二层真正做：

```text
symbol extraction
AST/callgraph check
test-node discovery
runtime trace artifact check
platform result check
divergence gate
```

### 状态

**PARTIAL。**

---

# 6. No Self-Authored Oracle：仍未完全落实

当前 Compact parity tests 仍读取：

```text
src/reference_data/ts_compression_layers.json
```

该文件本质上仍是项目自己维护的 snapshot。

### 风险

如果 snapshot 本身错误：

```text
Python implementation
→ 匹配错误 snapshot
→ tests green
→ 错误 VERIFIED
```

### 建议

将 snapshot 改成：

```yaml
reference_commit:
source_file:
source_sha:
symbols:
extractor_version:
extracted_facts:
```

由 source extractor 生成，不允许人工直接编辑行为结论。

### 状态

**OPEN。**

---

# 7. Durable Resume：本轮没有真正推进功能闭环

当前已经正确承认：

```text
same-process true resume = yes
restart-survivable durable resume = no
```

当前核心限制仍是：

```text
resume_run_params
```

属于内存 runtime dependency recipe，不持久化。

### 建议继续按 Reference 重构

恢复路径应是：

```text
durable transcript / metadata
→ sanitize messages
→ content replacement reconstruction
→ worktree restore/validation
→ current agent definition resolve
→ current provider/runtime factory
→ current tool pool
→ current permission context
→ current system prompt
→ canonical run_agent
```

### 必须新增测试

1. 进程级 registry 重建模拟；
2. 不携带原 `RunAgentParams`；
3. transcript/meta 足够时可恢复；
4. provider/tool factory 缺失时明确失败；
5. session temporary trust 不恢复；
6. worktree missing/recovered；
7. concurrent resume winner-only。

### 状态

**OPEN / P0。**

---

# 8. Isolation：B4 最大硬缺口仍未解决

当前仍是：

```text
macOS: real Seatbelt subset
Linux: NoSandboxBackend
Windows: NoSandboxBackend
```

并且：

```text
allowed_network_hosts
```

还未形成真正 domain policy enforcement。

### 建议

## macOS

继续补：

- managed/user/project sandbox config translation；
- protected settings files；
- `.claude/skills`；
- bare git escape；
- read allow/deny；
- allowed/denied domains；
- Unix sockets；
- local binding；
- proxy；
- background secret scrub。

## Linux

建立真实 backend：

```text
Landlock / bubblewrap / equivalent
```

至少验证：

- FS read/write；
- network；
- process tree；
- env；
- capability unavailable fail-closed。

## Windows

真实 backend 不能只靠 Git Bash process group。

需要：

```text
Job Object
+ Restricted Token / AppContainer / ACL
+ network policy
```

或等价可证明组合。

### 状态

**BLOCKED / P0。**

---

# 9. Scheduler：仍需真正 Source Differential

当前 Python Scheduler 功能较丰富，但 Reference 生命周期还有：

```text
file-backed tasks
filesystem watch
scheduler lock
owner takeover
lastFiredAt
missed one-shot
session/file task split
daemon/filter/killswitch
```

### 建议

不要继续以“功能比 Reference 多”作为等价证明。

建立状态机矩阵：

| Reference State/Event | Python State/Event | Equivalent | Divergence |
|---|---|---|---|
| file task load | snapshot/restore | ? | 必须证明 |
| owner lock | Python ownership | ? | 必须证明 |
| missed one-shot | Python due semantics | ? | 必须证明 |
| lastFiredAt | Python state | ? | 必须证明 |
| restart | snapshot restore | ? | 必须证明 |

如果采用不同 persistence 架构，应标：

```text
PYTHON_ADAPTATION_VERIFIED
```

而不是 `EXACT`。

### 状态

**OPEN。**

---

# 10. Cross-Surface：当前仍只有 Core ↔ Server

当前 runtime differential 已经比 B4 前好，但实际覆盖：

```text
core API
↔ DirectConnect server
```

尚未覆盖：

- CLI；
- TUI；
- Desktop；
- IDE/VSCode。

### 建议

使用同一 deterministic scripted provider：

```text
same prompt
same settings
same permission response
same model chunks
same tool outputs
same transcript seed
```

逐 Surface 比较：

- model call count；
- tool visibility；
- permission decision；
- tool execution order；
- state transitions；
- transcript；
- abort；
- terminal；
- usage；
- turns。

允许差异只在：

```text
rendering
transport envelope
surface-specific UI events
```

### 状态

**OPEN。**

---

# 11. `docs/status/current.md` 仍需要事实清理

当前该文件虽然已经使用新的 7×5×14 taxonomy，但仍保留大量 2026-08-11 migration baseline 描述。

这类历史信息不是错误，但不适合继续放在 `CURRENT` truth document 的核心区域。

### 建议

拆成：

```text
docs/status/current.md
→ 只保留当前 HEAD / 当前 tests / 当前 blockers

docs/history/...
→ migration baseline / old broken links / old collection errors
```

### 状态

**OPEN / Governance。**

---

# 12. Current-HEAD CI：仍未解决

目前只能确认：

```text
local Python suite green
```

不能确认：

```text
current HEAD GitHub CI green
```

且当前主分支没有 required status checks 证据。

### 建议

Final Exit 前必须建立至少：

```text
Linux Python core/parity
macOS Python + native sandbox
Windows Python + native isolation
docs/evidence checker
TUI/Desktop/IDE supported-surface tests
package/install smoke
```

并让 required checks 进入 branch protection。

### 状态

**OPEN / Final Gate。**

---

# 13. B4 问题复核总表 v2

| ID | B4 问题 | 当前状态 | 本轮评价 | 后续动作 |
|---|---|---|---|---|
| P01 | Resume 假恢复 | CLOSED | 保持正确 | 回归 |
| P02 | Resume race | CLOSED | 保持正确 | 回归 |
| P03 | Resume 内存 recipe | OPEN | 状态降级正确 | Durable reconstruction |
| P04 | 无真实 Sandbox | PARTIAL | macOS only | Linux/Windows |
| P05 | Foreground sandbox bypass | CLOSED | 保持正确 | 回归 |
| P06 | Background sandbox bypass | CLOSED | 保持正确 | 回归 |
| P07 | process-tree 生命周期 | PARTIAL | 主体已改善 | Canonical ProcessPolicy |
| P08 | filesystem isolation | PARTIAL | macOS 已有 | 跨平台 |
| P09 | network isolation | PARTIAL | binary deny 已有 | domain enforcement |
| P10 | secret/sandbox 联动 | PARTIAL | 子集覆盖 | background/跨平台 |
| P11 | MCP discovery 假 capability | CLOSED | 保持正确 | 回归 |
| P12 | MCP stale tools | CLOSED | 保持正确 | 回归 |
| P13 | Hook timeout child process | CLOSED | 保持正确 | Fault regression |
| P14 | Compact source drift | PARTIAL | **显著改善** | Snip + MC default + cache path |
| P15 | Snip | OPEN | 未解决 | Source recovery |
| P16 | Context-9 | PARTIAL | **显著改善** | Managed/User/Nested/Reload |
| P17 | Scheduler lifecycle | OPEN | 仅状态纠正 | Source state differential |
| P18 | Full cross-surface | OPEN | core/server 仍有限 | CLI/TUI/Desktop/IDE |
| P19 | async test 假绿 | CLOSED | 保持正确 | 测试治理 |
| P20 | Hook mock 参数 | CLOSED | 保持正确 | 回归 |
| P21 | sandbox unit/E2E 混淆 | CLOSED | 保持正确 | 分层测试 |
| P22 | background notification race | CLOSED | 保持正确 | 回归 |
| P23 | Machine evidence 矛盾 | PARTIAL | **大幅改善** | 原子生成 + Proof Engine |
| P24 | stale baseline | OPEN | **暴露 self-reference 问题** | subject/evidence commit 模型 |
| P25 | 双 taxonomy | CLOSED/基本关闭 | 本轮完成较好 | 禁止反复 |
| P26 | current HEAD CI | OPEN | 未解决 | Required CI |

---

# 14. 建议下一批修复顺序

本轮之后，不建议立刻同时铺开所有大功能。推荐顺序：

```text
R1  Machine Evidence subject/evidence commit 模型
↓
R2  Microcompact 默认值 + Snip 事实状态纠正
↓
R3  Path Rules：Managed/User/Nested + full-query E2E
↓
R4  Durable Resume
↓
R5  Scheduler lifecycle differential
↓
R6  Linux / Windows Real Sandbox
↓
R7  Full Surface Differential
↓
R8  Current-HEAD CI + Final machine scorecard
```

其中 R1 必须先做，否则后续每次提交都可能让 machine evidence 的 baseline 再次过期。

---

# 15. 下一批提交的验收标准

## R1 Evidence Control Plane

必须满足：

```text
no self-referential HEAD baseline
subject_commit explicit
evidence generated atomically
cross-file contradictions = 0
unproven VERIFIED rejected
scorecard derived, not hand-counted
```

## R2 Compact

必须满足：

```text
Microcompact default = reference default
Snip state = source-supported
No self-authored oracle
cache/time/no-op paths source-traceable
```

## R3 Context Rules

必须满足：

```text
Managed
User
Project CWD-level
Project nested-directory
single injection
concurrent dedup
symlink protection
compact reload
resume reload
next-model-call E2E
```

## R4 Durable Resume

必须满足：

```text
no dependency on persisted live object
restart-survivable reconstruction
current runtime factory
current tool/permission/system prompt
worktree
content replacement
trust reset
canonical run_agent
```

## R5 Scheduler

必须满足：

```text
session task
durable task
restart
missed one-shot
owner
lastFiredAt
cancel/reschedule
permission
isolation
```

## R6 Isolation

必须满足：

```text
macOS native
Linux native
Windows native
filesystem
network
env/secret
process tree
fail-closed
native CI
```

## R7 Surface

必须满足所有正式支持 Surface 进入 shared core 的 runtime differential。

---

# 16. 不建议做的事情

在上述遗留问题关闭前，不建议：

- 新增 provider；
- 扩展新的 workflow 产品功能；
- 新建第二 Agent Loop；
- 为提高 scorecard 把 PARTIAL 改 VERIFIED；
- 继续维护人工 reference snapshot 作为唯一 oracle；
- 把 macOS-only sandbox 写成 cross-platform complete；
- 把 same-process resume 写成 durable resume；
- 用 core/server 两路径代表全部 Surface；
- 用本地 pytest green 代表 GitHub CI green；
- 在 `docs/status/current.md` 混合大量历史 baseline。

---

# 17. 本轮最终反馈

这次针对 B4 遗留问题的修复，**最值得确认的是项目开始从“完成声明驱动”转为“事实证据驱动”**。

当前可以明确接受的改进：

```text
Compact early-return 修正
Microcompact 调用位置修正
Project path-scoped rules 真接线
Context / Permission 语义纠正
Machine evidence 主动降级
Evidence consistency checker
唯一 taxonomy
```

当前不能接受为最终完成的部分：

```text
Snip
完整 Context-9
Durable Resume
Cross-platform Isolation
Scheduler source lifecycle
Full Surface parity
Current-HEAD CI
Final machine evidence
```

因此当前最合适的项目状态仍然是：

```text
SOURCE_ALIGNED_CORE_IN_PROGRESS
```

而不是：

```text
SOURCE_ALIGNED_CORE_COMPLETE
```

v2 反馈的核心要求可以归纳为一句话：

> **继续修功能，但更重要的是让每一个“完成”都能由 Source → Python → Runtime → Test → Machine Evidence 五段连续证明，而不是由文档和测试数量共同推断。**
