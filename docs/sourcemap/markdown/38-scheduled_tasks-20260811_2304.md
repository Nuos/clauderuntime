# scheduled_tasks 源码盘点
> 编号: 38
> 时间戳: 20260811_2304
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: scheduled_tasks
2. 类型: package（目录模块）
3. Python 文件数: 3
4. 总行数: 648
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `scheduled_tasks/__init__.py` — 30 行（类 0，函数 0）
1. `scheduled_tasks/cron_expr.py` — 229 行（类 1，函数 3）
1. `scheduled_tasks/scheduler.py` — 389 行（类 4，函数 2）

## 3. 关键符号

1. 文件 `scheduled_tasks/__init__.py`
1. 文件 `scheduled_tasks/cron_expr.py`
1. 类 CronExpression — L82
1. 函数 _parse_field — L42
1. 函数 _step_of — L172
1. 函数 describe_cron — L188
1. 文件 `scheduled_tasks/scheduler.py`
1. 类 CronJob — L82
1. 类 PendingWakeup — L108
1. 类 FiredTask — L124
1. 类 SessionCronScheduler — L138
1. 函数 scheduled_tasks_disabled — L64
1. 函数 _stable_offset — L72

## 4. 依赖与调用边（import 级）

1. scheduled_tasks/__init__.py → import scheduled_tasks（外部）
1. scheduled_tasks/cron_expr.py → import __future__（外部）
1. scheduled_tasks/cron_expr.py → import dataclasses（外部）
1. scheduled_tasks/cron_expr.py → import datetime（外部）
1. scheduled_tasks/scheduler.py → import __future__（外部）
1. scheduled_tasks/scheduler.py → import hashlib（外部）
1. scheduled_tasks/scheduler.py → import threading（外部）
1. scheduled_tasks/scheduler.py → import time（外部）
1. scheduled_tasks/scheduler.py → import uuid（外部）
1. scheduled_tasks/scheduler.py → import dataclasses（外部）
1. scheduled_tasks/scheduler.py → import datetime（外部）
1. scheduled_tasks/scheduler.py → import typing（外部）
1. scheduled_tasks/scheduler.py → import src（外部）
1. scheduled_tasks/scheduler.py → import scheduled_tasks（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. CCR-14 Runtime Config / Feature Gate Control Plane — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → CCR-14（UNVERIFIED）
1. `cron_expr.py` → CCR-14（UNVERIFIED）
1. `scheduler.py` → CCR-14（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
