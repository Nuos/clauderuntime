# upstreamproxy 源码盘点
> 编号: 50
> 时间戳: 20260811_2304
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: upstreamproxy
2. 类型: package（目录模块）
3. Python 文件数: 6
4. 总行数: 1065
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `upstreamproxy/__init__.py` — 22 行（类 0，函数 0）
1. `upstreamproxy/ca_bundle.py` — 162 行（类 0，函数 2）
1. `upstreamproxy/protobuf_codec.py` — 95 行（类 0，函数 2）
1. `upstreamproxy/ptrace_guard.py` — 92 行（类 0，函数 1）
1. `upstreamproxy/relay.py` — 408 行（类 2，函数 6）
1. `upstreamproxy/upstream_proxy.py` — 286 行（类 1，函数 6）

## 3. 关键符号

1. 文件 `upstreamproxy/__init__.py`
1. 文件 `upstreamproxy/ca_bundle.py`
1. 函数 is_valid_pem_content — L45
1. 函数 download_ca_bundle — L67
1. 文件 `upstreamproxy/protobuf_codec.py`
1. 函数 encode_chunk — L34
1. 函数 decode_chunk — L56
1. 文件 `upstreamproxy/ptrace_guard.py`
1. 函数 set_non_dumpable — L35
1. 文件 `upstreamproxy/relay.py`
1. 类 UpstreamProxyRelay — L60
1. 类 _ConnectRequest — L76
1. 函数 _read_connect_request — L84
1. 函数 _pump_client_to_ws — L123
1. 函数 _pump_ws_to_client — L155
1. 函数 _keepalive — L189
1. 函数 _handle_connection — L211
1. 函数 start_upstream_proxy_relay — L361
1. 文件 `upstreamproxy/upstream_proxy.py`
1. 类 UpstreamProxyState — L60
1. 函数 _default_ca_bundle_out — L55
1. 函数 _is_env_truthy — L81
1. 函数 init_upstream_proxy — L93
1. 函数 get_upstream_proxy_env — L196
1. 函数 reset_for_tests — L251
1. 函数 _read_token — L257

## 4. 依赖与调用边（import 级）

1. upstreamproxy/__init__.py → import __future__（外部）
1. upstreamproxy/__init__.py → import json（外部）
1. upstreamproxy/__init__.py → import pathlib（外部）
1. upstreamproxy/ca_bundle.py → import __future__（外部）
1. upstreamproxy/ca_bundle.py → import logging（外部）
1. upstreamproxy/ca_bundle.py → import os（外部）
1. upstreamproxy/ca_bundle.py → import re（外部）
1. upstreamproxy/ca_bundle.py → import tempfile（外部）
1. upstreamproxy/ca_bundle.py → import pathlib（外部）
1. upstreamproxy/ca_bundle.py → import httpx（外部）
1. upstreamproxy/protobuf_codec.py → import __future__（外部）
1. upstreamproxy/ptrace_guard.py → import __future__（外部）
1. upstreamproxy/ptrace_guard.py → import logging（外部）
1. upstreamproxy/ptrace_guard.py → import platform（外部）
1. upstreamproxy/relay.py → import __future__（外部）
1. upstreamproxy/relay.py → import asyncio（外部）
1. upstreamproxy/relay.py → import base64（外部）
1. upstreamproxy/relay.py → import logging（外部）
1. upstreamproxy/relay.py → import collections（外部）
1. upstreamproxy/relay.py → import dataclasses（外部）
1. upstreamproxy/relay.py → import websockets（外部）
1. upstreamproxy/relay.py → import upstreamproxy（外部）
1. upstreamproxy/upstream_proxy.py → import __future__（外部）
1. upstreamproxy/upstream_proxy.py → import logging（外部）
1. upstreamproxy/upstream_proxy.py → import os（外部）
1. upstreamproxy/upstream_proxy.py → import dataclasses（外部）
1. upstreamproxy/upstream_proxy.py → import pathlib（外部）
1. upstreamproxy/upstream_proxy.py → import src（外部）
1. upstreamproxy/upstream_proxy.py → import upstreamproxy（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. R5-05 Backend Layer — 候选（启发式），状态 UNVERIFIED
1. CCR-12 Isolation Runtime — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → R5-05, CCR-12（UNVERIFIED）
1. `ca_bundle.py` → R5-05, CCR-12（UNVERIFIED）
1. `protobuf_codec.py` → R5-05, CCR-12（UNVERIFIED）
1. `ptrace_guard.py` → R5-05, CCR-12（UNVERIFIED）
1. `relay.py` → R5-05, CCR-12（UNVERIFIED）
1. `upstream_proxy.py` → R5-05, CCR-12（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
