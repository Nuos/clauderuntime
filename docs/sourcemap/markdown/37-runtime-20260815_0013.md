# runtime 源码盘点
> 编号: 37
> 时间戳: 20260815_0013
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: runtime
2. 类型: package（目录模块）
3. Python 文件数: 8
4. 总行数: 1085
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `runtime/__init__.py` — 9 行（类 0，函数 0）
1. `runtime/completion_verifier.py` — 105 行（类 4，函数 0）
1. `runtime/extension_activation.py` — 301 行（类 5，函数 3）
1. `runtime/legacy_task_projection.py` — 109 行（类 1，函数 0）
1. `runtime/model_capability_resolver.py` — 108 行（类 2，函数 2）
1. `runtime/server_session_facades.py` — 67 行（类 5，函数 0）
1. `runtime/session_lifecycle.py` — 160 行（类 3，函数 0）
1. `runtime/turn_preparation.py` — 226 行（类 2，函数 0）

## 3. 关键符号

1. 文件 `runtime/__init__.py`
1. 文件 `runtime/completion_verifier.py`
1. 类 CompletionStatus — L22
1. 类 CompletionDecision — L29
1. 类 CompletionVerifier — L36
1. 类 StructuralCompletionVerifier — L51
1. 文件 `runtime/extension_activation.py`
1. 类 ActivationBehavior — L64
1. 类 ExtensionDescriptor — L73
1. 类 ActivationDecision — L92
1. 类 _LedgerEntry — L98
1. 类 ExtensionActivationGate — L104
1. 函数 _load_policy — L52
1. 函数 content_hash — L271
1. 函数 descriptor_from_loaded_plugin — L281
1. 文件 `runtime/legacy_task_projection.py`
1. 类 LegacyTaskProjection — L24
1. 文件 `runtime/model_capability_resolver.py`
1. 类 ModelCapabilities — L21
1. 类 ModelCapabilityResolver — L49
1. 函数 _matches_any — L42
1. 函数 resolve_model_capabilities — L93
1. 文件 `runtime/server_session_facades.py`
1. 类 PermissionBridge — L21
1. 类 SurfaceEmitter — L29
1. 类 SchedulerBridge — L40
1. 类 SessionState — L47
1. 类 ServerSessionFacades — L61
1. 文件 `runtime/session_lifecycle.py`
1. 类 SessionPhase — L47
1. 类 SessionLifecycleRecord — L56
1. 类 SessionLifecycle — L64
1. 文件 `runtime/turn_preparation.py`
1. 类 PreparedTurn — L34
1. 类 TurnPreparationService — L52

## 4. 依赖与调用边（import 级）

1. runtime/completion_verifier.py → import __future__（外部）
1. runtime/completion_verifier.py → import dataclasses（外部）
1. runtime/completion_verifier.py → import enum（外部）
1. runtime/completion_verifier.py → import typing（外部）
1. runtime/extension_activation.py → import __future__（外部）
1. runtime/extension_activation.py → import hashlib（外部）
1. runtime/extension_activation.py → import logging（外部）
1. runtime/extension_activation.py → import dataclasses（外部）
1. runtime/extension_activation.py → import enum（外部）
1. runtime/extension_activation.py → import pathlib（外部）
1. runtime/extension_activation.py → import typing（外部）
1. runtime/legacy_task_projection.py → import __future__（外部）
1. runtime/legacy_task_projection.py → import collections（外部）
1. runtime/legacy_task_projection.py → import typing（外部）
1. runtime/legacy_task_projection.py → import src（外部）
1. runtime/model_capability_resolver.py → import __future__（外部）
1. runtime/model_capability_resolver.py → import re（外部）
1. runtime/model_capability_resolver.py → import dataclasses（外部）
1. runtime/model_capability_resolver.py → import typing（外部）
1. runtime/server_session_facades.py → import __future__（外部）
1. runtime/server_session_facades.py → import dataclasses（外部）
1. runtime/server_session_facades.py → import typing（外部）
1. runtime/session_lifecycle.py → import __future__（外部）
1. runtime/session_lifecycle.py → import logging（外部）
1. runtime/session_lifecycle.py → import dataclasses（外部）
1. runtime/session_lifecycle.py → import enum（外部）
1. runtime/session_lifecycle.py → import typing（外部）
1. runtime/turn_preparation.py → import __future__（外部）
1. runtime/turn_preparation.py → import dataclasses（外部）
1. runtime/turn_preparation.py → import typing（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. R7-03 Agent Loop — 候选（启发式），状态 UNVERIFIED
1. R7-06 State & Persistence — 候选（启发式），状态 UNVERIFIED
1. R5-02 Core Layer — 候选（启发式），状态 UNVERIFIED
1. R5-04 State Layer — 候选（启发式），状态 UNVERIFIED
1. CCR-06 Tool Orchestration — 候选（启发式），状态 UNVERIFIED
1. CCR-14 Runtime Config / Feature Gate Control Plane — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → R7-03, R7-06, R5-02, R5-04, CCR-06, CCR-14（UNVERIFIED）
1. `completion_verifier.py` → R7-03, R7-06, R5-02, R5-04, CCR-06, CCR-14（UNVERIFIED）
1. `extension_activation.py` → R7-03, R7-06, R5-02, R5-04, CCR-06, CCR-14（UNVERIFIED）
1. `legacy_task_projection.py` → R7-03, R7-06, R5-02, R5-04, CCR-06, CCR-14（UNVERIFIED）
1. `model_capability_resolver.py` → R7-03, R7-06, R5-02, R5-04, CCR-06, CCR-14（UNVERIFIED）
1. `server_session_facades.py` → R7-03, R7-06, R5-02, R5-04, CCR-06, CCR-14（UNVERIFIED）
1. `session_lifecycle.py` → R7-03, R7-06, R5-02, R5-04, CCR-06, CCR-14（UNVERIFIED）
1. `turn_preparation.py` → R7-03, R7-06, R5-02, R5-04, CCR-06, CCR-14（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
