# cli_core 源码盘点
> 编号: 09
> 时间戳: 20260811_2302
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: cli_core
2. 类型: package（目录模块）
3. Python 文件数: 4
4. 总行数: 351
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `cli_core/__init__.py` — 41 行（类 0，函数 0）
1. `cli_core/exit.py` — 28 行（类 0，函数 2）
1. `cli_core/ndjson.py` — 37 行（类 0，函数 1）
1. `cli_core/structured_io.py` — 245 行（类 11，函数 1）

## 3. 关键符号

1. 文件 `cli_core/__init__.py`
1. 文件 `cli_core/exit.py`
1. 函数 cli_error — L14
1. 函数 cli_ok — L22
1. 文件 `cli_core/ndjson.py`
1. 函数 ndjson_safe_dumps — L24
1. 文件 `cli_core/structured_io.py`
1. 类 HeadlessEvent — L33
1. 类 SystemEvent — L44
1. 类 PartialTextEvent — L56
1. 类 AssistantEvent — L62
1. 类 ToolUseEvent — L69
1. 类 ToolResultEvent — L77
1. 类 UsageEvent — L86
1. 类 ResultEvent — L130
1. 类 UserInputMessage — L147
1. 类 StreamJsonReader — L162
1. 类 StreamJsonWriter — L228
1. 函数 _extract_text — L204

## 4. 依赖与调用边（import 级）

1. cli_core/__init__.py → import cli_error（外部）
1. cli_core/__init__.py → import cli_ok（外部）
1. cli_core/__init__.py → import ndjson_safe_dumps（外部）
1. cli_core/__init__.py → import AssistantEvent（外部）
1. cli_core/__init__.py → import HeadlessEvent（外部）
1. cli_core/__init__.py → import PartialTextEvent（外部）
1. cli_core/__init__.py → import ResultEvent（外部）
1. cli_core/__init__.py → import StreamJsonReader（外部）
1. cli_core/__init__.py → import StreamJsonWriter（外部）
1. cli_core/__init__.py → import SystemEvent（外部）
1. cli_core/__init__.py → import ToolResultEvent（外部）
1. cli_core/__init__.py → import UsageEvent（外部）
1. cli_core/__init__.py → import ToolUseEvent（外部）
1. cli_core/__init__.py → import UserInputMessage（外部）
1. cli_core/exit.py → import annotations（外部）
1. cli_core/exit.py → import sys（外部）
1. cli_core/exit.py → import NoReturn（外部）
1. cli_core/ndjson.py → import annotations（外部）
1. cli_core/ndjson.py → import json（外部）
1. cli_core/ndjson.py → import Any（外部）
1. cli_core/structured_io.py → import annotations（外部）
1. cli_core/structured_io.py → import json（外部）
1. cli_core/structured_io.py → import sys（外部）
1. cli_core/structured_io.py → import asdict（外部）
1. cli_core/structured_io.py → import dataclass（外部）
1. cli_core/structured_io.py → import field（外部）
1. cli_core/structured_io.py → import Any（外部）
1. cli_core/structured_io.py → import IO（外部）
1. cli_core/structured_io.py → import Iterable（外部）
1. cli_core/structured_io.py → import Iterator（外部）
1. cli_core/structured_io.py → import ndjson_safe_dumps（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. R7-02 Interfaces — 候选（启发式），状态 UNVERIFIED
1. R5-01 Surface Layer — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → R7-02, R5-01（UNVERIFIED）
1. `exit.py` → R7-02, R5-01（UNVERIFIED）
1. `ndjson.py` → R7-02, R5-01（UNVERIFIED）
1. `structured_io.py` → R7-02, R5-01（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
