# settings 源码盘点
> 编号: 43
> 时间戳: 20260811_2304
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: settings
2. 类型: package（目录模块）
3. Python 文件数: 8
4. 总行数: 957
5. 依赖的 src 内部模块: config
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `settings/__init__.py` — 27 行（类 0，函数 0）
1. `settings/change_detector.py` — 74 行（类 2，函数 1）
1. `settings/constants.py` — 67 行（类 0，函数 0）
1. `settings/managed_path.py` — 45 行（类 0，函数 1）
1. `settings/permission_validation.py` — 38 行（类 0，函数 1）
1. `settings/settings.py` — 192 行（类 0，函数 5）
1. `settings/types.py` — 371 行（类 10，函数 0）
1. `settings/validation.py` — 143 行（类 1，函数 1）

## 3. 关键符号

1. 文件 `settings/__init__.py`
1. 文件 `settings/change_detector.py`
1. 类 SettingsDiff — L13
1. 类 SettingsChangeDetector — L24
1. 函数 _diff_dicts — L49
1. 文件 `settings/constants.py`
1. 文件 `settings/managed_path.py`
1. 函数 resolve_managed_settings_path — L12
1. 文件 `settings/permission_validation.py`
1. 函数 validate_permission_rules — L11
1. 文件 `settings/settings.py`
1. 函数 invalidate_settings_cache — L19
1. 函数 load_settings — L25
1. 函数 get_settings — L58
1. 函数 get_persisted_model — L70
1. 函数 update_local_settings — L135
1. 文件 `settings/types.py`
1. 类 PermissionRule — L13
1. 类 ToolSettings — L24
1. 类 OutputStyleSettings — L33
1. 类 SpinnerVerbsSettings — L41
1. 类 SandboxSettings — L53
1. 类 CompactSettings — L78
1. 类 ModelLimitSettings — L86
1. 类 HookSettings — L94
1. 类 McpServerSettings — L102
1. 类 SettingsSchema — L111
1. 文件 `settings/validation.py`
1. 类 ValidationError — L17
1. 函数 validate_settings — L24

## 4. 依赖与调用边（import 级）

1. settings/__init__.py → import __future__（外部）
1. settings/__init__.py → import settings（外部）
1. settings/change_detector.py → import __future__（外部）
1. settings/change_detector.py → import dataclasses（外部）
1. settings/change_detector.py → import typing（外部）
1. settings/change_detector.py → import settings（外部）
1. settings/constants.py → import __future__（外部）
1. settings/constants.py → import settings（外部）
1. settings/managed_path.py → import __future__（外部）
1. settings/managed_path.py → import os（外部）
1. settings/managed_path.py → import pathlib（外部）
1. settings/permission_validation.py → import __future__（外部）
1. settings/permission_validation.py → import re（外部）
1. settings/permission_validation.py → import typing（外部）
1. settings/permission_validation.py → import settings（外部）
1. settings/settings.py → import __future__（外部）
1. settings/settings.py → import dataclasses（外部）
1. settings/settings.py → import logging（外部）
1. settings/settings.py → import pathlib（外部）
1. settings/settings.py → import typing（外部）
1. settings/settings.py → import config（内部）
1. settings/settings.py → import settings（外部）
1. settings/types.py → import __future__（外部）
1. settings/types.py → import dataclasses（外部）
1. settings/types.py → import typing（外部）
1. settings/validation.py → import __future__（外部）
1. settings/validation.py → import dataclasses（外部）
1. settings/validation.py → import typing（外部）
1. settings/validation.py → import settings（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. CCR-14 Runtime Config / Feature Gate Control Plane — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → CCR-14（UNVERIFIED）
1. `change_detector.py` → CCR-14（UNVERIFIED）
1. `constants.py` → CCR-14（UNVERIFIED）
1. `managed_path.py` → CCR-14（UNVERIFIED）
1. `permission_validation.py` → CCR-14（UNVERIFIED）
1. `settings.py` → CCR-14（UNVERIFIED）
1. `types.py` → CCR-14（UNVERIFIED）
1. `validation.py` → CCR-14（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
