# entrypoints 源码盘点
> 编号: 18
> 时间戳: 20260811_2302
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: entrypoints
2. 类型: package（目录模块）
3. Python 文件数: 12
4. 总行数: 3360
5. 依赖的 src 内部模块: Server
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `entrypoints/__init__.py` — 52 行（类 0，函数 1）
1. `entrypoints/agent_server_cli.py` — 475 行（类 0，函数 6）
1. `entrypoints/daemon.py` — 28 行（类 0，函数 1）
1. `entrypoints/desktop_cli.py` — 129 行（类 0，函数 6）
1. `entrypoints/doctor.py` — 44 行（类 0，函数 1）
1. `entrypoints/headless.py` — 1333 行（类 2，函数 12）
1. `entrypoints/mcp.py` — 89 行（类 0，函数 3）
1. `entrypoints/mcp_serve.py` — 343 行（类 0，函数 9）
1. `entrypoints/migrate.py` — 60 行（类 0，函数 1）
1. `entrypoints/provider_validation.py` — 86 行（类 0，函数 2）
1. `entrypoints/serve_cli.py` — 315 行（类 0，函数 5）
1. `entrypoints/tui_launcher.py` — 406 行（类 0，函数 9）

## 3. 关键符号

1. 文件 `entrypoints/__init__.py`
1. 函数 __getattr__ — L31
1. 文件 `entrypoints/agent_server_cli.py`
1. 函数 _exit_when_stdin_closes — L64
1. 函数 _sanitize_agent_server_mode — L86
1. 函数 run_agent_server_subcommand — L110
1. 函数 _serve_stdio — L306
1. 函数 _maybe_init_upstream_proxy — L395
1. 函数 _serve — L433
1. 文件 `entrypoints/daemon.py`
1. 函数 run_daemon_subcommand — L16
1. 文件 `entrypoints/desktop_cli.py`
1. 函数 repo_root — L32
1. 函数 desktop_dir — L37
1. 函数 launch_env — L41
1. 函数 build_launch_plan — L48
1. 函数 dev_port_busy — L61
1. 函数 run_desktop_subcommand — L78
1. 文件 `entrypoints/doctor.py`
1. 函数 run_doctor — L13
1. 文件 `entrypoints/headless.py`
1. 类 HeadlessOptions — L158
1. 类 _InAgentLoopFlag — L1102
1. 函数 _accumulate_usage — L68
1. 函数 _billed_token_total — L106
1. 函数 _json_result_subtype — L111
1. 函数 _resolve_headless_max_turns — L141
1. 函数 _resolve_headless_max_cost — L150
1. 函数 run_headless — L205
1. 函数 _install_sigint_handler — L1120
1. 函数 _filter_registry — L1214
1. 函数 _auto_deny_permission_handler — L1238
1. 函数 _noop_ask_user — L1256
1. 函数 _build_event_bridge — L1279
1. 函数 _jsonable — L1321
1. 文件 `entrypoints/mcp.py`
1. 函数 run_mcp_subcommand — L16
1. 函数 _print_usage — L54
1. 函数 _list_servers — L62
1. 文件 `entrypoints/mcp_serve.py`
1. 函数 _build_serve_context — L58
1. 函数 get_combined_tools — L96
1. 函数 load_reexposed_mcp_tools — L105
1. 函数 _tool_description — L132
1. 函数 _result_to_content — L144
1. 函数 _error_result — L180
1. 函数 build_server — L187
1. 函数 start_mcp_server — L320
1. 函数 run_serve — L330
1. 文件 `entrypoints/migrate.py`
1. 函数 run_migrate_subcommand — L29
1. 文件 `entrypoints/provider_validation.py`
1. 函数 get_provider_validation_error — L33
1. 函数 validate_provider_at_startup — L63
1. 文件 `entrypoints/serve_cli.py`
1. 函数 _build_parser — L66
1. 函数 _exit_when_stdin_closes — L114
1. 函数 run_serve_subcommand — L132
1. 函数 _announce_ready — L227
1. 函数 _serve — L249
1. 文件 `entrypoints/tui_launcher.py`
1. 函数 run_tui_launcher — L48
1. 函数 launch_ink_tui — L187
1. 函数 _resolve_tui_dir — L241
1. 函数 _resolve_tui_command — L258
1. 函数 _agent_server_cmd — L282
1. 函数 _child_env — L312
1. 函数 _parent_ignores_sigint — L335
1. 函数 _print_connect — L354
1. 函数 _launch — L377

## 4. 依赖与调用边（import 级）

1. entrypoints/__init__.py → import annotations（外部）
1. entrypoints/__init__.py → import json（外部）
1. entrypoints/__init__.py → import Path（外部）
1. entrypoints/agent_server_cli.py → import annotations（外部）
1. entrypoints/agent_server_cli.py → import argparse（外部）
1. entrypoints/agent_server_cli.py → import asyncio（外部）
1. entrypoints/agent_server_cli.py → import json（外部）
1. entrypoints/agent_server_cli.py → import logging（外部）
1. entrypoints/agent_server_cli.py → import os（外部）
1. entrypoints/agent_server_cli.py → import sys（外部）
1. entrypoints/agent_server_cli.py → import threading（外部）
1. entrypoints/agent_server_cli.py → import Path（外部）
1. entrypoints/agent_server_cli.py → import profile_checkpoint（外部）
1. entrypoints/agent_server_cli.py → import DEFAULT_MAX_TURNS（外部）
1. entrypoints/agent_server_cli.py → import AgentServerConfig（外部）
1. entrypoints/agent_server_cli.py → import PROTOCOL_VERSION（外部）
1. entrypoints/agent_server_cli.py → import make_spawn_agent（外部）
1. entrypoints/agent_server_cli.py → import DirectConnectServer（外部）
1. entrypoints/agent_server_cli.py → import SessionManager（外部）
1. entrypoints/agent_server_cli.py → import ServerConfig（外部）
1. entrypoints/daemon.py → import annotations（外部）
1. entrypoints/daemon.py → import sys（外部）
1. entrypoints/desktop_cli.py → import annotations（外部）
1. entrypoints/desktop_cli.py → import argparse（外部）
1. entrypoints/desktop_cli.py → import os（外部）
1. entrypoints/desktop_cli.py → import shutil（外部）
1. entrypoints/desktop_cli.py → import subprocess（外部）
1. entrypoints/desktop_cli.py → import sys（外部）
1. entrypoints/desktop_cli.py → import Path（外部）
1. entrypoints/doctor.py → import annotations（外部）
1. entrypoints/doctor.py → import sys（外部）
1. entrypoints/headless.py → import annotations（外部）
1. entrypoints/headless.py → import io（外部）
1. entrypoints/headless.py → import os（外部）
1. entrypoints/headless.py → import signal（外部）
1. entrypoints/headless.py → import sys（外部）
1. entrypoints/headless.py → import time（外部）
1. entrypoints/headless.py → import dataclass（外部）
1. entrypoints/headless.py → import Path（外部）
1. entrypoints/headless.py → import IO（外部）
1. entrypoints/headless.py → import Callable（外部）
1. entrypoints/headless.py → import Iterable（外部）
1. entrypoints/headless.py → import Optional（外部）
1. entrypoints/headless.py → import Session（外部）
1. entrypoints/headless.py → import AssistantEvent（外部）
1. entrypoints/headless.py → import PartialTextEvent（外部）
1. entrypoints/headless.py → import ResultEvent（外部）
1. entrypoints/headless.py → import StreamJsonReader（外部）
1. entrypoints/headless.py → import StreamJsonWriter（外部）
1. entrypoints/headless.py → import SystemEvent（外部）
1. entrypoints/headless.py → import ToolResultEvent（外部）
1. entrypoints/headless.py → import ToolUseEvent（外部）
1. entrypoints/headless.py → import UsageEvent（外部）
1. entrypoints/headless.py → import UserInputMessage（外部）
1. entrypoints/headless.py → import cli_error（外部）
1. entrypoints/headless.py → import ndjson_safe_dumps（外部）
1. entrypoints/headless.py → import get_default_provider（外部）
1. entrypoints/headless.py → import get_provider_config（外部）
1. entrypoints/headless.py → import get_provider_class（外部）
1. entrypoints/headless.py → import resolve_api_key（外部）
1. entrypoints/headless.py → import AgentLoopResult（外部）
1. entrypoints/headless.py → import ToolEvent（外部）
1. entrypoints/headless.py → import build_effective_system_prompt（外部）
1. entrypoints/headless.py → import run_query_as_agent_loop（外部）
1. entrypoints/headless.py → import ToolContext（外部）
1. entrypoints/headless.py → import build_default_registry（外部）
1. entrypoints/headless.py → import EARLY_STOP_SUBTYPES（外部）
1. entrypoints/headless.py → import AbortController（外部）
1. entrypoints/headless.py → import AbortError（外部）
1. entrypoints/mcp.py → import annotations（外部）
1. entrypoints/mcp.py → import sys（外部）
1. entrypoints/mcp_serve.py → import annotations（外部）
1. entrypoints/mcp_serve.py → import asyncio（外部）
1. entrypoints/mcp_serve.py → import json（外部）
1. entrypoints/mcp_serve.py → import logging（外部）
1. entrypoints/mcp_serve.py → import sys（外部）
1. entrypoints/mcp_serve.py → import Path（外部）
1. entrypoints/mcp_serve.py → import Any（外部）
1. entrypoints/mcp_serve.py → import Iterable（外部）
1. entrypoints/mcp_serve.py → import mcp（外部）
1. entrypoints/mcp_serve.py → import Server（内部）
1. entrypoints/mcp_serve.py → import stdio_server（外部）
1. entrypoints/mcp_serve.py → import __version__（外部）
1. entrypoints/migrate.py → import annotations（外部）
1. entrypoints/migrate.py → import os（外部）
1. entrypoints/provider_validation.py → import annotations（外部）
1. entrypoints/provider_validation.py → import sys（外部）
1. entrypoints/serve_cli.py → import annotations（外部）
1. entrypoints/serve_cli.py → import argparse（外部）
1. entrypoints/serve_cli.py → import asyncio（外部）
1. entrypoints/serve_cli.py → import json（外部）
1. entrypoints/serve_cli.py → import logging（外部）
1. entrypoints/serve_cli.py → import os（外部）
1. entrypoints/serve_cli.py → import secrets（外部）
1. entrypoints/serve_cli.py → import sys（外部）
1. entrypoints/serve_cli.py → import Path（外部）
1. entrypoints/serve_cli.py → import profile_checkpoint（外部）
1. entrypoints/serve_cli.py → import DEFAULT_MAX_TURNS（外部）
1. entrypoints/serve_cli.py → import AgentServerConfig（外部）
1. entrypoints/serve_cli.py → import PROTOCOL_VERSION（外部）
1. entrypoints/serve_cli.py → import make_spawn_agent（外部）
1. entrypoints/serve_cli.py → import SessionManager（外部）
1. entrypoints/tui_launcher.py → import annotations（外部）
1. entrypoints/tui_launcher.py → import argparse（外部）
1. entrypoints/tui_launcher.py → import asyncio（外部）
1. entrypoints/tui_launcher.py → import contextlib（外部）
1. entrypoints/tui_launcher.py → import os（外部）
1. entrypoints/tui_launcher.py → import secrets（外部）
1. entrypoints/tui_launcher.py → import shutil（外部）
1. entrypoints/tui_launcher.py → import signal（外部）
1. entrypoints/tui_launcher.py → import sys（外部）
1. entrypoints/tui_launcher.py → import Path（外部）
1. entrypoints/tui_launcher.py → import SimpleNamespace（外部）
1. entrypoints/tui_launcher.py → import run_agent_server_subcommand（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. R7-02 Interfaces — 候选（启发式），状态 UNVERIFIED
1. R5-01 Surface Layer — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → R7-02, R5-01（UNVERIFIED）
1. `agent_server_cli.py` → R7-02, R5-01（UNVERIFIED）
1. `daemon.py` → R7-02, R5-01（UNVERIFIED）
1. `desktop_cli.py` → R7-02, R5-01（UNVERIFIED）
1. `doctor.py` → R7-02, R5-01（UNVERIFIED）
1. `headless.py` → R7-02, R5-01（UNVERIFIED）
1. `mcp.py` → R7-02, R5-01（UNVERIFIED）
1. `mcp_serve.py` → R7-02, R5-01（UNVERIFIED）
1. `migrate.py` → R7-02, R5-01（UNVERIFIED）
1. `provider_validation.py` → R7-02, R5-01（UNVERIFIED）
1. `serve_cli.py` → R7-02, R5-01（UNVERIFIED）
1. `tui_launcher.py` → R7-02, R5-01（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
