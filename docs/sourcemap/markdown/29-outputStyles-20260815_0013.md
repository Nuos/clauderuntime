# outputStyles 源码盘点
> 编号: 29
> 时间戳: 20260815_0013
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: outputStyles
2. 类型: package（目录模块）
3. Python 文件数: 3
4. 总行数: 238
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `outputStyles/__init__.py` — 33 行（类 0，函数 0）
1. `outputStyles/loader.py` — 179 行（类 0，函数 7）
1. `outputStyles/styles.py` — 26 行（类 1，函数 0）

## 3. 关键符号

1. 文件 `outputStyles/__init__.py`
1. 文件 `outputStyles/loader.py`
1. 函数 _user_style_dirs — L32
1. 函数 _load_default_user_styles — L50
1. 函数 load_output_styles_dir — L59
1. 函数 resolve_output_style — L106
1. 函数 _coerce_str — L131
1. 函数 output_style_from_settings — L145
1. 函数 available_output_styles — L164
1. 文件 `outputStyles/styles.py`
1. 类 OutputStyle — L8

## 4. 依赖与调用边（import 级）

1. outputStyles/__init__.py → import __future__（外部）
1. outputStyles/__init__.py → import json（外部）
1. outputStyles/__init__.py → import pathlib（外部）
1. outputStyles/__init__.py → import outputStyles（外部）
1. outputStyles/loader.py → import __future__（外部）
1. outputStyles/loader.py → import logging（外部）
1. outputStyles/loader.py → import pathlib（外部）
1. outputStyles/loader.py → import src（外部）
1. outputStyles/loader.py → import outputStyles（外部）
1. outputStyles/styles.py → import __future__（外部）
1. outputStyles/styles.py → import dataclasses（外部）
1. outputStyles/styles.py → import pathlib（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. CCR-14 Runtime Config / Feature Gate Control Plane — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → CCR-14（UNVERIFIED）
1. `loader.py` → CCR-14（UNVERIFIED）
1. `styles.py` → CCR-14（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
