# background 源码盘点
> 编号: 04
> 时间戳: 20260811_2302
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: background
2. 类型: package（目录模块）
3. Python 文件数: 2
4. 总行数: 121
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `background/__init__.py` — 8 行（类 0，函数 0）
1. `background/tasks.py` — 113 行（类 2，函数 0）

## 3. 关键符号

1. 文件 `background/__init__.py`
1. 文件 `background/tasks.py`
1. 类 BgTask — L22
1. 类 BackgroundTasks — L31

## 4. 依赖与调用边（import 级）

1. background/__init__.py → import BackgroundTasks（外部）
1. background/__init__.py → import BgTask（外部）
1. background/tasks.py → import annotations（外部）
1. background/tasks.py → import subprocess（外部）
1. background/tasks.py → import sys（外部）
1. background/tasks.py → import threading（外部）
1. background/tasks.py → import uuid（外部）
1. background/tasks.py → import dataclass（外部）
1. background/tasks.py → import bash_env（外部）
1. background/tasks.py → import find_bash（外部）
1. background/tasks.py → import kill_process_tree（外部）
1. background/tasks.py → import popen_tree_kwargs（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. CCR-11 Subagent Orchestration — 候选（启发式），状态 UNVERIFIED
1. CCR-10 Session / Transcript Runtime — 候选（启发式），状态 UNVERIFIED
1. CCR-14 Runtime Config / Feature Gate Control Plane — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → CCR-11, CCR-10, CCR-14（UNVERIFIED）
1. `tasks.py` → CCR-11, CCR-10, CCR-14（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
