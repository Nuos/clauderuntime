# agent 源码盘点
> 编号: 01
> 时间戳: 20260811_2302
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: agent
2. 类型: package（目录模块）
3. Python 文件数: 18
4. 总行数: 3879
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `agent/__init__.py` — 119 行（类 0，函数 0）
1. `agent/agent_definitions.py` — 293 行（类 1，函数 6）
1. `agent/agent_model.py` — 112 行（类 0，函数 2）
1. `agent/agent_tool_utils.py` — 362 行（类 2，函数 9）
1. `agent/constants.py` — 82 行（类 0，函数 0）
1. `agent/conversation.py` — 102 行（类 1，函数 1）
1. `agent/filter_agents_by_mcp.py` — 49 行（类 0，函数 2）
1. `agent/foreground_promotion.py` — 219 行（类 0，函数 5）
1. `agent/fork_subagent.py` — 297 行（类 0，函数 7）
1. `agent/load_agents_dir.py` — 157 行（类 0，函数 5）
1. `agent/load_plugin_agents.py` — 109 行（类 0，函数 3）
1. `agent/parse_agent_markdown.py` — 228 行（类 0，函数 8）
1. `agent/prompt.py` — 224 行（类 0，函数 4）
1. `agent/resume_agent.py` — 246 行（类 1，函数 3）
1. `agent/run_agent.py` — 448 行（类 2，函数 4）
1. `agent/session.py` — 156 行（类 1，函数 1）
1. `agent/subagent_context.py` — 270 行（类 1，函数 2）
1. `agent/transcript.py` — 406 行（类 2，函数 7）

## 3. 关键符号

1. 文件 `agent/__init__.py`
1. 文件 `agent/agent_definitions.py`
1. 类 AgentDefinition — L17
1. 函数 _general_purpose_system_prompt — L79
1. 函数 _explore_system_prompt — L104
1. 函数 _plan_system_prompt — L170
1. 函数 get_built_in_agents — L253
1. 函数 is_built_in_agent — L280
1. 函数 find_agent_by_type — L285
1. 文件 `agent/agent_model.py`
1. 函数 _resolve_against_provider — L33
1. 函数 get_agent_model — L90
1. 文件 `agent/agent_tool_utils.py`
1. 类 ResolvedAgentTools — L42
1. 类 AgentToolResult — L191
1. 函数 get_query_source_for_agent — L27
1. 函数 filter_tools_for_agent — L51
1. 函数 resolve_agent_tools — L97
1. 函数 count_tool_uses — L201
1. 函数 finalize_agent_tool — L214
1. 函数 _resolve_total_tokens — L283
1. 函数 extract_partial_result — L329
1. 函数 _extract_tool_name — L349
1. 函数 _extract_rule_content — L357
1. 文件 `agent/constants.py`
1. 文件 `agent/conversation.py`
1. 类 Conversation — L27
1. 函数 _normalize_message_content — L97
1. 文件 `agent/filter_agents_by_mcp.py`
1. 函数 has_required_mcp_servers — L16
1. 函数 filter_agents_by_mcp_requirements — L35
1. 文件 `agent/foreground_promotion.py`
1. 函数 register_agent_foreground — L57
1. 函数 register_agent_background — L79
1. 函数 unregister_agent_foreground — L107
1. 函数 run_with_background_escape — L124
1. 函数 _anext — L208
1. 文件 `agent/fork_subagent.py`
1. 函数 is_fork_subagent_enabled — L64
1. 函数 is_in_fork_child — L101
1. 函数 build_child_message — L131
1. 函数 _collect_tool_use_blocks — L172
1. 函数 build_forked_messages — L183
1. 函数 build_worktree_notice — L256
1. 函数 _get_attr — L275
1. 文件 `agent/load_agents_dir.py`
1. 函数 _cache_key — L62
1. 函数 clear_agent_definitions_cache — L69
1. 函数 get_active_agents_from_list — L74
1. 函数 _load_custom_agents — L92
1. 函数 get_agent_definitions_with_overrides — L115
1. 文件 `agent/load_plugin_agents.py`
1. 函数 _scan_md_files — L29
1. 函数 _build_namespaced_agent_type — L54
1. 函数 load_plugin_agents — L64
1. 文件 `agent/parse_agent_markdown.py`
1. 函数 _first — L59
1. 函数 _parse_tools — L67
1. 函数 _parse_color — L83
1. 函数 _parse_memory — L90
1. 函数 _parse_isolation — L103
1. 函数 _parse_bool — L116
1. 函数 _parse_model — L124
1. 函数 parse_agent_from_markdown — L134
1. 文件 `agent/prompt.py`
1. 函数 _get_tools_description — L20
1. 函数 format_agent_line — L42
1. 函数 get_agent_prompt — L51
1. 函数 get_agent_system_prompt — L200
1. 文件 `agent/resume_agent.py`
1. 类 ResumeResult — L59
1. 函数 _claim_resume — L80
1. 函数 _reconstruct_messages_from_transcript — L129
1. 函数 resume_agent_background — L141
1. 文件 `agent/run_agent.py`
1. 类 RunAgentParams — L42
1. 类 RunAgentResult — L79
1. 函数 resolve_permission_mode — L89
1. 函数 _build_permission_context — L117
1. 函数 filter_incomplete_tool_calls — L173
1. 函数 run_agent — L227
1. 文件 `agent/session.py`
1. 类 Session — L40
1. 函数 _snapshot_cost_block — L145
1. 文件 `agent/subagent_context.py`
1. 类 SubagentContextOverrides — L26
1. 函数 create_subagent_context — L57
1. 函数 _wrap_avoid_prompts — L258
1. 文件 `agent/transcript.py`
1. 类 TranscriptWriter — L224
1. 类 TranscriptReader — L315
1. 函数 _transcripts_root — L81
1. 函数 get_agent_transcript_path — L94
1. 函数 get_workflow_run_path — L105
1. 函数 _sanitize_agent_id — L118
1. 函数 _serialize_message — L142
1. 函数 _append_lock — L177
1. 函数 ensure_transcript_dir — L391

## 4. 依赖与调用边（import 级）

1. agent/__init__.py → import Conversation（外部）
1. agent/__init__.py → import Message（外部）
1. agent/__init__.py → import Session（外部）
1. agent/__init__.py → import AgentDefinition（外部）
1. agent/__init__.py → import AgentSource（外部）
1. agent/__init__.py → import BuiltInAgentDefinition（外部）
1. agent/__init__.py → import EXPLORE_AGENT（外部）
1. agent/__init__.py → import FORK_AGENT（外部）
1. agent/__init__.py → import GENERAL_PURPOSE_AGENT（外部）
1. agent/__init__.py → import PLAN_AGENT（外部）
1. agent/__init__.py → import find_agent_by_type（外部）
1. agent/__init__.py → import get_built_in_agents（外部）
1. agent/__init__.py → import is_built_in_agent（外部）
1. agent/__init__.py → import AgentToolResult（外部）
1. agent/__init__.py → import ResolvedAgentTools（外部）
1. agent/__init__.py → import count_tool_uses（外部）
1. agent/__init__.py → import extract_partial_result（外部）
1. agent/__init__.py → import filter_tools_for_agent（外部）
1. agent/__init__.py → import finalize_agent_tool（外部）
1. agent/__init__.py → import resolve_agent_tools（外部）
1. agent/__init__.py → import AGENT_TOOL_NAME（外部）
1. agent/__init__.py → import ALL_AGENT_DISALLOWED_TOOLS（外部）
1. agent/__init__.py → import ASYNC_AGENT_ALLOWED_TOOLS（外部）
1. agent/__init__.py → import CUSTOM_AGENT_DISALLOWED_TOOLS（外部）
1. agent/__init__.py → import DEFAULT_AGENT_PROMPT（外部）
1. agent/__init__.py → import FORK_SUBAGENT_TYPE（外部）
1. agent/__init__.py → import LEGACY_AGENT_TOOL_NAME（外部）
1. agent/__init__.py → import ONE_SHOT_BUILTIN_AGENT_TYPES（外部）
1. agent/__init__.py → import filter_agents_by_mcp_requirements（外部）
1. agent/__init__.py → import has_required_mcp_servers（外部）
1. agent/__init__.py → import clear_agent_definitions_cache（外部）
1. agent/__init__.py → import get_active_agents_from_list（外部）
1. agent/__init__.py → import get_agent_definitions_with_overrides（外部）
1. agent/__init__.py → import load_plugin_agents（外部）
1. agent/__init__.py → import parse_agent_from_markdown（外部）
1. agent/__init__.py → import format_agent_line（外部）
1. agent/__init__.py → import get_agent_prompt（外部）
1. agent/__init__.py → import get_agent_system_prompt（外部）
1. agent/__init__.py → import RunAgentParams（外部）
1. agent/__init__.py → import RunAgentResult（外部）
1. agent/__init__.py → import filter_incomplete_tool_calls（外部）
1. agent/__init__.py → import resolve_permission_mode（外部）
1. agent/__init__.py → import run_agent（外部）
1. agent/__init__.py → import SubagentContextOverrides（外部）
1. agent/__init__.py → import create_subagent_context（外部）
1. agent/agent_definitions.py → import annotations（外部）
1. agent/agent_definitions.py → import dataclass（外部）
1. agent/agent_definitions.py → import field（外部）
1. agent/agent_definitions.py → import Any（外部）
1. agent/agent_definitions.py → import Callable（外部）
1. agent/agent_definitions.py → import Literal（外部）
1. agent/agent_definitions.py → import PermissionMode（外部）
1. agent/agent_model.py → import annotations（外部）
1. agent/agent_model.py → import logging（外部）
1. agent/agent_model.py → import os（外部）
1. agent/agent_model.py → import Any（外部）
1. agent/agent_tool_utils.py → import annotations（外部）
1. agent/agent_tool_utils.py → import logging（外部）
1. agent/agent_tool_utils.py → import time（外部）
1. agent/agent_tool_utils.py → import dataclass（外部）
1. agent/agent_tool_utils.py → import field（外部）
1. agent/agent_tool_utils.py → import Any（外部）
1. agent/agent_tool_utils.py → import PermissionMode（外部）
1. agent/agent_tool_utils.py → import Tool（外部）
1. agent/agent_tool_utils.py → import Tools（外部）
1. agent/agent_tool_utils.py → import tool_matches_name（外部）
1. agent/agent_tool_utils.py → import ToolUseBlock（外部）
1. agent/agent_tool_utils.py → import AssistantMessage（外部）
1. agent/agent_tool_utils.py → import Message（外部）
1. agent/agent_tool_utils.py → import AGENT_TOOL_NAME（外部）
1. agent/agent_tool_utils.py → import ALL_AGENT_DISALLOWED_TOOLS（外部）
1. agent/agent_tool_utils.py → import ASYNC_AGENT_ALLOWED_TOOLS（外部）
1. agent/agent_tool_utils.py → import CUSTOM_AGENT_DISALLOWED_TOOLS（外部）
1. agent/constants.py → import annotations（外部）
1. agent/conversation.py → import annotations（外部）
1. agent/conversation.py → import dataclass（外部）
1. agent/conversation.py → import field（外部）
1. agent/conversation.py → import Any（外部）
1. agent/conversation.py → import ContentBlock（外部）
1. agent/conversation.py → import TextBlock（外部）
1. agent/conversation.py → import ToolResultBlock（外部）
1. agent/conversation.py → import ToolUseBlock（外部）
1. agent/conversation.py → import normalize_content_blocks（外部）
1. agent/conversation.py → import Message（外部）
1. agent/conversation.py → import MessageContent（外部）
1. agent/conversation.py → import create_message（外部）
1. agent/conversation.py → import create_user_message（外部）
1. agent/conversation.py → import message_from_dict（外部）
1. agent/conversation.py → import message_to_dict（外部）
1. agent/conversation.py → import normalize_messages_for_api（外部）
1. agent/filter_agents_by_mcp.py → import annotations（外部）
1. agent/filter_agents_by_mcp.py → import Iterable（外部）
1. agent/filter_agents_by_mcp.py → import AgentDefinition（外部）
1. agent/filter_agents_by_mcp.py → import is_built_in_agent（外部）
1. agent/foreground_promotion.py → import annotations（外部）
1. agent/foreground_promotion.py → import asyncio（外部）
1. agent/foreground_promotion.py → import logging（外部）
1. agent/foreground_promotion.py → import replace（外部）
1. agent/foreground_promotion.py → import Any（外部）
1. agent/foreground_promotion.py → import AsyncIterator（外部）
1. agent/foreground_promotion.py → import TYPE_CHECKING（外部）
1. agent/foreground_promotion.py → import LocalAgentTaskState（外部）
1. agent/fork_subagent.py → import annotations（外部）
1. agent/fork_subagent.py → import copy（外部）
1. agent/fork_subagent.py → import os（外部）
1. agent/fork_subagent.py → import Any（外部）
1. agent/fork_subagent.py → import uuid4（外部）
1. agent/fork_subagent.py → import get_is_non_interactive_session（外部）
1. agent/fork_subagent.py → import ToolContext（外部）
1. agent/fork_subagent.py → import TextBlock（外部）
1. agent/fork_subagent.py → import ToolResultBlock（外部）
1. agent/fork_subagent.py → import ToolUseBlock（外部）
1. agent/fork_subagent.py → import AssistantMessage（外部）
1. agent/fork_subagent.py → import Message（外部）
1. agent/fork_subagent.py → import UserMessage（外部）
1. agent/fork_subagent.py → import create_user_message（外部）
1. agent/fork_subagent.py → import FORK_AGENT（外部）
1. agent/fork_subagent.py → import FORK_BOILERPLATE_TAG（外部）
1. agent/fork_subagent.py → import FORK_DIRECTIVE_PREFIX（外部）
1. agent/fork_subagent.py → import FORK_SUBAGENT_TYPE（外部）
1. agent/fork_subagent.py → import is_env_truthy（外部）
1. agent/load_agents_dir.py → import annotations（外部）
1. agent/load_agents_dir.py → import logging（外部）
1. agent/load_agents_dir.py → import os（外部）
1. agent/load_agents_dir.py → import Iterable（外部）
1. agent/load_agents_dir.py → import AgentDefinition（外部）
1. agent/load_agents_dir.py → import get_built_in_agents（外部）
1. agent/load_agents_dir.py → import parse_agent_from_markdown（外部）
1. agent/load_agents_dir.py → import SOURCE_MANAGED（外部）
1. agent/load_agents_dir.py → import SOURCE_PROJECT（外部）
1. agent/load_agents_dir.py → import SOURCE_USER（外部）
1. agent/load_agents_dir.py → import load_markdown_files_for_subdir（外部）
1. agent/load_plugin_agents.py → import annotations（外部）
1. agent/load_plugin_agents.py → import logging（外部）
1. agent/load_plugin_agents.py → import replace（外部）
1. agent/load_plugin_agents.py → import Path（外部）
1. agent/load_plugin_agents.py → import AgentDefinition（外部）
1. agent/load_plugin_agents.py → import parse_agent_from_markdown（外部）
1. agent/load_plugin_agents.py → import LoadedPlugin（外部）
1. agent/load_plugin_agents.py → import parse_frontmatter（外部）
1. agent/parse_agent_markdown.py → import annotations（外部）
1. agent/parse_agent_markdown.py → import logging（外部）
1. agent/parse_agent_markdown.py → import Path（外部）
1. agent/parse_agent_markdown.py → import Any（外部）
1. agent/parse_agent_markdown.py → import AgentDefinition（外部）
1. agent/parse_agent_markdown.py → import AgentSource（外部）
1. agent/parse_agent_markdown.py → import parse_effort_value（外部）
1. agent/parse_agent_markdown.py → import parse_hooks（外部）
1. agent/parse_agent_markdown.py → import parse_permission_mode（外部）
1. agent/parse_agent_markdown.py → import parse_positive_int（外部）
1. agent/parse_agent_markdown.py → import parse_string_list（外部）
1. agent/prompt.py → import annotations（外部）
1. agent/prompt.py → import TYPE_CHECKING（外部）
1. agent/resume_agent.py → import annotations（外部）
1. agent/resume_agent.py → import logging（外部）
1. agent/resume_agent.py → import dataclass（外部）
1. agent/resume_agent.py → import replace（外部）
1. agent/resume_agent.py → import Any（外部）
1. agent/resume_agent.py → import TYPE_CHECKING（外部）
1. agent/resume_agent.py → import TranscriptReader（外部）
1. agent/resume_agent.py → import LocalAgentTaskState（外部）
1. agent/resume_agent.py → import register_async_agent（外部）
1. agent/resume_agent.py → import is_terminal_task_status（外部）
1. agent/run_agent.py → import annotations（外部）
1. agent/run_agent.py → import copy（外部）
1. agent/run_agent.py → import logging（外部）
1. agent/run_agent.py → import time（外部）
1. agent/run_agent.py → import dataclass（外部）
1. agent/run_agent.py → import field（外部）
1. agent/run_agent.py → import Any（外部）
1. agent/run_agent.py → import AsyncGenerator（外部）
1. agent/run_agent.py → import uuid4（外部）
1. agent/run_agent.py → import PermissionMode（外部）
1. agent/run_agent.py → import ToolPermissionContext（外部）
1. agent/run_agent.py → import BaseProvider（外部）
1. agent/run_agent.py → import Tools（外部）
1. agent/run_agent.py → import ToolContext（外部）
1. agent/run_agent.py → import ToolRegistry（外部）
1. agent/run_agent.py → import ToolUseBlock（外部）
1. agent/run_agent.py → import AssistantMessage（外部）
1. agent/run_agent.py → import Message（外部）
1. agent/run_agent.py → import UserMessage（外部）
1. agent/run_agent.py → import AbortController（外部）
1. agent/run_agent.py → import AgentDefinition（外部）
1. agent/run_agent.py → import is_built_in_agent（外部）
1. agent/run_agent.py → import count_tool_uses（外部）
1. agent/run_agent.py → import get_query_source_for_agent（外部）
1. agent/run_agent.py → import resolve_agent_tools（外部）
1. agent/run_agent.py → import AGENT_TOOL_NAME（外部）
1. agent/run_agent.py → import get_agent_system_prompt（外部）
1. agent/run_agent.py → import SubagentContextOverrides（外部）
1. agent/run_agent.py → import create_subagent_context（外部）
1. agent/session.py → import annotations（外部）
1. agent/session.py → import json（外部）
1. agent/session.py → import time（外部）
1. agent/session.py → import datetime（外部）
1. agent/session.py → import Optional（外部）
1. agent/session.py → import dataclass（外部）
1. agent/session.py → import field（外部）
1. agent/session.py → import get_sessions_dir（外部）
1. agent/session.py → import get_model_usage（外部）
1. agent/session.py → import get_session_id（外部）
1. agent/session.py → import get_start_time（外部）
1. agent/session.py → import get_total_api_duration（外部）
1. agent/session.py → import get_total_api_duration_without_retries（外部）
1. agent/session.py → import get_total_cost_usd（外部）
1. agent/session.py → import get_total_lines_added（外部）
1. agent/session.py → import get_total_lines_removed（外部）
1. agent/session.py → import get_total_tool_duration（外部）
1. agent/session.py → import Conversation（外部）
1. agent/subagent_context.py → import annotations（外部）
1. agent/subagent_context.py → import copy（外部）
1. agent/subagent_context.py → import logging（外部）
1. agent/subagent_context.py → import dataclass（外部）
1. agent/subagent_context.py → import field（外部）
1. agent/subagent_context.py → import Any（外部）
1. agent/subagent_context.py → import uuid4（外部）
1. agent/subagent_context.py → import ToolPermissionContext（外部）
1. agent/subagent_context.py → import FileReadingLimits（外部）
1. agent/subagent_context.py → import QueryChainTracking（外部）
1. agent/subagent_context.py → import ToolContext（外部）
1. agent/subagent_context.py → import ToolUseOptions（外部）
1. agent/subagent_context.py → import AbortController（外部）
1. agent/subagent_context.py → import create_child_abort_controller（外部）
1. agent/transcript.py → import annotations（外部）
1. agent/transcript.py → import errno（外部）
1. agent/transcript.py → import json（外部）
1. agent/transcript.py → import logging（外部）
1. agent/transcript.py → import os（外部）
1. agent/transcript.py → import time（外部）
1. agent/transcript.py → import contextmanager（外部）
1. agent/transcript.py → import asdict（外部）
1. agent/transcript.py → import is_dataclass（外部）
1. agent/transcript.py → import Path（外部）
1. agent/transcript.py → import Any（外部）
1. agent/transcript.py → import Iterator（外部）
1. agent/transcript.py → import get_transcripts_dir（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. R7-03 Agent Loop — 候选（启发式），状态 UNVERIFIED
1. R5-02 Core Layer — 候选（启发式），状态 UNVERIFIED
1. CCR-11 Subagent Orchestration — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `agent_definitions.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `agent_model.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `agent_tool_utils.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `constants.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `conversation.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `filter_agents_by_mcp.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `foreground_promotion.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `fork_subagent.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `load_agents_dir.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `load_plugin_agents.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `parse_agent_markdown.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `prompt.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `resume_agent.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `run_agent.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `session.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `subagent_context.py` → R7-03, R5-02, CCR-11（UNVERIFIED）
1. `transcript.py` → R7-03, R5-02, CCR-11（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
