# goals 源码盘点
> 编号: 20
> 时间戳: 20260811_2304
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: goals
2. 类型: package（目录模块）
3. Python 文件数: 3
4. 总行数: 1317
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `goals/__init__.py` — 44 行（类 0，函数 0）
1. `goals/command.py` — 206 行（类 1，函数 2）
1. `goals/goals.py` — 1067 行（类 3，函数 10）

## 3. 关键符号

1. 文件 `goals/__init__.py`
1. 文件 `goals/command.py`
1. 类 GoalCommandResult — L26
1. 函数 run_goal_command — L46
1. 函数 run_subgoal_command — L131
1. 文件 `goals/goals.py`
1. 类 GoalState — L178
1. 类 GoalJudgeTimeout — L324
1. 类 GoalManager — L604
1. 函数 _truncate — L249
1. 函数 _parse_judge_response — L260
1. 函数 judge_goal — L328
1. 函数 build_judge_callable — L395
1. 函数 _resolve_judge_model — L462
1. 函数 collect_turn_evidence — L483
1. 函数 _block_get — L580
1. 函数 _flatten_block_text — L586
1. 函数 _fmt_duration — L1029
1. 函数 _fmt_tokens — L1040

## 4. 依赖与调用边（import 级）

1. goals/__init__.py → import src（外部）
1. goals/command.py → import __future__（外部）
1. goals/command.py → import dataclasses（外部）
1. goals/command.py → import typing（外部）
1. goals/command.py → import src（外部）
1. goals/goals.py → import __future__（外部）
1. goals/goals.py → import json（外部）
1. goals/goals.py → import logging（外部）
1. goals/goals.py → import re（外部）
1. goals/goals.py → import time（外部）
1. goals/goals.py → import dataclasses（外部）
1. goals/goals.py → import datetime（外部）
1. goals/goals.py → import typing（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. R7-03 Agent Loop — 候选（启发式），状态 UNVERIFIED
1. R5-02 Core Layer — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → R7-03, R5-02（UNVERIFIED）
1. `command.py` → R7-03, R5-02（UNVERIFIED）
1. `goals.py` → R7-03, R5-02（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
