# execution 源码盘点
> 编号: 19
> 时间戳: 20260811_2302
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: execution
2. 类型: package（目录模块）
3. Python 文件数: 4
4. 总行数: 635
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `execution/__init__.py` — 65 行（类 0，函数 0）
1. `execution/boundary.py` — 207 行（类 9，函数 2）
1. `execution/policy.py` — 161 行（类 4，函数 2）
1. `execution/sandbox.py` — 202 行（类 7，函数 3）

## 3. 关键符号

1. 文件 `execution/__init__.py`
1. 文件 `execution/boundary.py`
1. 类 WorkspaceDecision — L26
1. 类 WorkspaceGuard — L32
1. 类 DefaultWorkspaceGuard — L44
1. 类 ProcessDecision — L84
1. 类 ProcessPolicy — L89
1. 类 DefaultProcessPolicy — L100
1. 类 EnvPolicy — L115
1. 类 DefaultEnvPolicy — L123
1. 类 ExecutionBoundary — L138
1. 函数 default_execution_boundary — L191
1. 函数 minimal_execution_boundary — L195
1. 文件 `execution/policy.py`
1. 类 MinimalEnvPolicy — L32
1. 类 NetworkDecision — L64
1. 类 NetworkPolicy — L72
1. 类 ConfigurableNetworkPolicy — L77
1. 函数 _is_loopback_host — L151
1. 函数 default_network_policy — L160
1. 文件 `execution/sandbox.py`
1. 类 SandboxCapability — L19
1. 类 SandboxPolicy — L28
1. 类 SandboxRequest — L35
1. 类 SandboxInvocation — L44
1. 类 SandboxExecutionResult — L54
1. 类 SandboxBackend — L61
1. 类 NoSandboxBackend — L76
1. 函数 current_sandbox_platform — L8
1. 函数 sandbox_policy_from_settings — L168
1. 函数 default_sandbox_backend — L201

## 4. 依赖与调用边（import 级）

1. execution/__init__.py → import DefaultEnvPolicy（外部）
1. execution/__init__.py → import DefaultProcessPolicy（外部）
1. execution/__init__.py → import DefaultWorkspaceGuard（外部）
1. execution/__init__.py → import EnvPolicy（外部）
1. execution/__init__.py → import ExecutionBoundary（外部）
1. execution/__init__.py → import ProcessDecision（外部）
1. execution/__init__.py → import ProcessPolicy（外部）
1. execution/__init__.py → import WorkspaceDecision（外部）
1. execution/__init__.py → import WorkspaceGuard（外部）
1. execution/__init__.py → import default_execution_boundary（外部）
1. execution/__init__.py → import minimal_execution_boundary（外部）
1. execution/__init__.py → import ConfigurableNetworkPolicy（外部）
1. execution/__init__.py → import DEFAULT_MINIMAL_ENV_KEYS（外部）
1. execution/__init__.py → import MinimalEnvPolicy（外部）
1. execution/__init__.py → import NetworkDecision（外部）
1. execution/__init__.py → import NetworkMode（外部）
1. execution/__init__.py → import NetworkPolicy（外部）
1. execution/__init__.py → import default_network_policy（外部）
1. execution/__init__.py → import NoSandboxBackend（外部）
1. execution/__init__.py → import SandboxBackend（外部）
1. execution/__init__.py → import SandboxCapability（外部）
1. execution/__init__.py → import SandboxExecutionResult（外部）
1. execution/__init__.py → import SandboxInvocation（外部）
1. execution/__init__.py → import SandboxPolicy（外部）
1. execution/__init__.py → import SandboxRequest（外部）
1. execution/__init__.py → import current_sandbox_platform（外部）
1. execution/__init__.py → import default_sandbox_backend（外部）
1. execution/__init__.py → import sandbox_policy_from_settings（外部）
1. execution/boundary.py → import annotations（外部）
1. execution/boundary.py → import dataclass（外部）
1. execution/boundary.py → import field（外部）
1. execution/boundary.py → import Path（外部）
1. execution/boundary.py → import Mapping（外部）
1. execution/boundary.py → import Protocol（外部）
1. execution/boundary.py → import Sequence（外部）
1. execution/boundary.py → import ConfigurableNetworkPolicy（外部）
1. execution/boundary.py → import MinimalEnvPolicy（外部）
1. execution/boundary.py → import NetworkDecision（外部）
1. execution/boundary.py → import NetworkMode（外部）
1. execution/boundary.py → import NetworkPolicy（外部）
1. execution/boundary.py → import default_network_policy（外部）
1. execution/boundary.py → import SandboxBackend（外部）
1. execution/boundary.py → import SandboxExecutionResult（外部）
1. execution/boundary.py → import SandboxInvocation（外部）
1. execution/boundary.py → import SandboxPolicy（外部）
1. execution/boundary.py → import SandboxRequest（外部）
1. execution/boundary.py → import default_sandbox_backend（外部）
1. execution/policy.py → import annotations（外部）
1. execution/policy.py → import dataclass（外部）
1. execution/policy.py → import field（外部）
1. execution/policy.py → import ip_address（外部）
1. execution/policy.py → import Literal（外部）
1. execution/policy.py → import Mapping（外部）
1. execution/policy.py → import Protocol（外部）
1. execution/policy.py → import urlparse（外部）
1. execution/policy.py → import SUBPROCESS_SECRET_ENV_KEYS（外部）
1. execution/sandbox.py → import annotations（外部）
1. execution/sandbox.py → import dataclass（外部）
1. execution/sandbox.py → import field（外部）
1. execution/sandbox.py → import Path（外部）
1. execution/sandbox.py → import Mapping（外部）
1. execution/sandbox.py → import Protocol（外部）
1. execution/sandbox.py → import Sequence（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. R7-07 Execution Environment — 候选（启发式），状态 UNVERIFIED
1. R5-05 Backend Layer — 候选（启发式），状态 UNVERIFIED
1. CCR-12 Isolation Runtime — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → R7-07, R5-05, CCR-12（UNVERIFIED）
1. `boundary.py` → R7-07, R5-05, CCR-12（UNVERIFIED）
1. `policy.py` → R7-07, R5-05, CCR-12（UNVERIFIED）
1. `sandbox.py` → R7-07, R5-05, CCR-12（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
