# models 源码盘点
> 编号: 27
> 时间戳: 20260811_2304
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: models
2. 类型: package（目录模块）
3. Python 文件数: 9
4. 总行数: 1316
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `models/__init__.py` — 56 行（类 0，函数 0）
1. `models/agent_routing.py` — 37 行（类 1，函数 1）
1. `models/aliases.py` — 38 行（类 0，函数 1）
1. `models/bedrock.py` — 31 行（类 0，函数 2）
1. `models/capabilities.py` — 70 行（类 1，函数 5）
1. `models/configs.py` — 789 行（类 1，函数 1）
1. `models/context.py` — 182 行（类 0，函数 6）
1. `models/model.py` — 49 行（类 0，函数 4）
1. `models/validation.py` — 64 行（类 0，函数 3）

## 3. 关键符号

1. 文件 `models/__init__.py`
1. 文件 `models/agent_routing.py`
1. 类 AgentModelConfig — L10
1. 函数 get_model_for_agent — L16
1. 文件 `models/aliases.py`
1. 函数 resolve_alias — L33
1. 文件 `models/bedrock.py`
1. 函数 to_bedrock_model_id — L24
1. 函数 from_bedrock_model_id — L29
1. 文件 `models/capabilities.py`
1. 类 ModelCapabilities — L11
1. 函数 get_model_capabilities — L20
1. 函数 supports_thinking — L38
1. 函数 supports_tools — L42
1. 函数 supports_vision — L46
1. 函数 supports_computer_use — L69
1. 文件 `models/configs.py`
1. 类 ModelConfig — L9
1. 函数 get_model_config — L762
1. 文件 `models/context.py`
1. 函数 has_1m_context_suffix — L25
1. 函数 strip_1m_context_suffix — L30
1. 函数 _settings_limit — L41
1. 函数 get_context_window_for_model — L100
1. 函数 get_model_max_output_tokens — L120
1. 函数 resolve_max_output_tokens — L141
1. 文件 `models/model.py`
1. 函数 resolve_model — L14
1. 函数 canonical_model_name — L30
1. 函数 display_name — L35
1. 函数 deprecation_warning — L44
1. 文件 `models/validation.py`
1. 函数 validate_model_name — L16
1. 函数 is_model_allowed — L37
1. 函数 _matches_pattern — L57

## 4. 依赖与调用边（import 级）

1. models/__init__.py → import __future__（外部）
1. models/__init__.py → import models（外部）
1. models/agent_routing.py → import __future__（外部）
1. models/agent_routing.py → import dataclasses（外部）
1. models/agent_routing.py → import typing（外部）
1. models/aliases.py → import __future__（外部）
1. models/bedrock.py → import __future__（外部）
1. models/capabilities.py → import __future__（外部）
1. models/capabilities.py → import dataclasses（外部）
1. models/capabilities.py → import models（外部）
1. models/configs.py → import __future__（外部）
1. models/configs.py → import dataclasses（外部）
1. models/context.py → import __future__（外部）
1. models/context.py → import logging（外部）
1. models/context.py → import os（外部）
1. models/context.py → import urllib（外部）
1. models/context.py → import models（外部）
1. models/model.py → import __future__（外部）
1. models/model.py → import logging（外部）
1. models/model.py → import typing（外部）
1. models/model.py → import models（外部）
1. models/validation.py → import __future__（外部）
1. models/validation.py → import re（外部）
1. models/validation.py → import models（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. CCR-14 Runtime Config / Feature Gate Control Plane — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → CCR-14（UNVERIFIED）
1. `agent_routing.py` → CCR-14（UNVERIFIED）
1. `aliases.py` → CCR-14（UNVERIFIED）
1. `bedrock.py` → CCR-14（UNVERIFIED）
1. `capabilities.py` → CCR-14（UNVERIFIED）
1. `configs.py` → CCR-14（UNVERIFIED）
1. `context.py` → CCR-14（UNVERIFIED）
1. `model.py` → CCR-14（UNVERIFIED）
1. `validation.py` → CCR-14（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
