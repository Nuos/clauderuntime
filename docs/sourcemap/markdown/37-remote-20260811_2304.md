# remote 源码盘点
> 编号: 37
> 时间戳: 20260811_2304
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: remote
2. 类型: package（目录模块）
3. Python 文件数: 4
4. 总行数: 908
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `remote/__init__.py` — 22 行（类 0，函数 0）
1. `remote/remote_session_manager.py` — 293 行（类 3，函数 1）
1. `remote/sdk_message_adapter.py` — 112 行（类 0，函数 3）
1. `remote/sessions_websocket.py` — 481 行（类 2，函数 1）

## 3. 关键符号

1. 文件 `remote/__init__.py`
1. 文件 `remote/remote_session_manager.py`
1. 类 RemoteSessionConfig — L38
1. 类 RemoteSessionCallbacks — L50
1. 类 RemoteSessionManager — L66
1. 函数 create_remote_session_config — L270
1. 文件 `remote/sdk_message_adapter.py`
1. 函数 adapt_wire_to_sdk — L33
1. 函数 adapt_sdk_to_wire_user_message — L45
1. 函数 adapt_permission_response — L74
1. 文件 `remote/sessions_websocket.py`
1. 类 SessionsWebSocketCallbacks — L75
1. 类 SessionsWebSocket — L99
1. 函数 _is_sessions_message — L89

## 4. 依赖与调用边（import 级）

1. remote/__init__.py → import __future__（外部）
1. remote/__init__.py → import json（外部）
1. remote/__init__.py → import pathlib（外部）
1. remote/remote_session_manager.py → import __future__（外部）
1. remote/remote_session_manager.py → import asyncio（外部）
1. remote/remote_session_manager.py → import logging（外部）
1. remote/remote_session_manager.py → import collections（外部）
1. remote/remote_session_manager.py → import dataclasses（外部）
1. remote/remote_session_manager.py → import src（外部）
1. remote/remote_session_manager.py → import remote（外部）
1. remote/sdk_message_adapter.py → import __future__（外部）
1. remote/sdk_message_adapter.py → import logging（外部）
1. remote/sdk_message_adapter.py → import typing（外部）
1. remote/sdk_message_adapter.py → import src（外部）
1. remote/sessions_websocket.py → import __future__（外部）
1. remote/sessions_websocket.py → import asyncio（外部）
1. remote/sessions_websocket.py → import json（外部）
1. remote/sessions_websocket.py → import logging（外部）
1. remote/sessions_websocket.py → import uuid（外部）
1. remote/sessions_websocket.py → import collections（外部）
1. remote/sessions_websocket.py → import dataclasses（外部）
1. remote/sessions_websocket.py → import typing（外部）
1. remote/sessions_websocket.py → import websockets（外部）
1. remote/sessions_websocket.py → import src（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. R5-05 Backend Layer — 候选（启发式），状态 UNVERIFIED
1. CCR-12 Isolation Runtime — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → R5-05, CCR-12（UNVERIFIED）
1. `remote_session_manager.py` → R5-05, CCR-12（UNVERIFIED）
1. `sdk_message_adapter.py` → R5-05, CCR-12（UNVERIFIED）
1. `sessions_websocket.py` → R5-05, CCR-12（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
