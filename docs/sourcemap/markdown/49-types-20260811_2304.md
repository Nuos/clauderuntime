# types 源码盘点
> 编号: 49
> 时间戳: 20260811_2304
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: types
2. 类型: package（目录模块）
3. Python 文件数: 4
4. 总行数: 1321
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `types/__init__.py` — 136 行（类 0，函数 0）
1. `types/content_blocks.py` — 231 行（类 7，函数 4）
1. `types/messages.py` — 823 行（类 6，函数 24）
1. `types/stream_events.py` — 131 行（类 6，函数 2）

## 3. 关键符号

1. 文件 `types/__init__.py`
1. 文件 `types/content_blocks.py`
1. 类 TextBlock — L10
1. 类 ToolUseBlock — L16
1. 类 ToolResultBlock — L24
1. 类 ThinkingBlock — L37
1. 类 RedactedThinkingBlock — L44
1. 类 ImageBlock — L50
1. 类 DocumentBlock — L56
1. 函数 content_block_from_dict — L78
1. 函数 normalize_content_blocks — L134
1. 函数 content_block_to_dict — L147
1. 函数 _is_content_block_instance — L215
1. 文件 `types/messages.py`
1. 类 Message — L48
1. 类 UserMessage — L61
1. 类 AssistantMessage — L73
1. 类 SystemMessage — L88
1. 类 ProgressMessage — L99
1. 类 AttachmentMessage — L110
1. 函数 create_user_message — L127
1. 函数 create_assistant_message — L162
1. 函数 create_assistant_api_error_message — L186
1. 函数 create_system_message — L206
1. 函数 create_progress_message — L226
1. 函数 create_attachment_message — L240
1. 函数 create_stop_hook_summary_message — L254
1. 函数 create_user_interruption_message — L282
1. 函数 create_message — L294
1. 函数 normalize_messages_for_api — L314
1. 函数 normalize_message_for_api — L335
1. 函数 ensure_tool_result_pairing — L365
1. 函数 _hoist_tool_results — L513
1. 函数 _join_text_at_seam — L519
1. 函数 _merge_user_api_messages — L545
1. 函数 _is_system_local_command — L561
1. 函数 message_to_dict — L565
1. 函数 message_from_dict — L611
1. 函数 is_tool_use_request_message — L726
1. 函数 is_tool_use_result_message — L739
1. 函数 get_tool_use_ids — L752
1. 函数 get_last_assistant_message — L763
1. 函数 _normalize_loaded_content — L770
1. 函数 _get_field — L778
1. 文件 `types/stream_events.py`
1. 类 MessageStart — L12
1. 类 ContentBlockStart — L18
1. 类 ContentBlockDelta — L25
1. 类 ContentBlockStop — L32
1. 类 MessageDelta — L38
1. 类 MessageStop — L45
1. 函数 stream_event_to_dict — L68
1. 函数 stream_event_from_dict — L98

## 4. 依赖与调用边（import 级）

1. types/__init__.py → import __future__（外部）
1. types/__init__.py → import json（外部）
1. types/__init__.py → import pathlib（外部）
1. types/__init__.py → import types（外部）
1. types/content_blocks.py → import __future__（外部）
1. types/content_blocks.py → import dataclasses（外部）
1. types/content_blocks.py → import typing（外部）
1. types/messages.py → import __future__（外部）
1. types/messages.py → import dataclasses（外部）
1. types/messages.py → import datetime（外部）
1. types/messages.py → import typing（外部）
1. types/messages.py → import uuid（外部）
1. types/messages.py → import types（外部）
1. types/stream_events.py → import __future__（外部）
1. types/stream_events.py → import dataclasses（外部）
1. types/stream_events.py → import typing（外部）
1. types/stream_events.py → import types（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. CCR-14 Runtime Config / Feature Gate Control Plane — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → CCR-14（UNVERIFIED）
1. `content_blocks.py` → CCR-14（UNVERIFIED）
1. `messages.py` → CCR-14（UNVERIFIED）
1. `stream_events.py` → CCR-14（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
