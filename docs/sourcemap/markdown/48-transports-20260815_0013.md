# transports 源码盘点
> 编号: 48
> 时间戳: 20260815_0013
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: transports
2. 类型: package（目录模块）
3. Python 文件数: 9
4. 总行数: 3146
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `transports/__init__.py` — 55 行（类 0，函数 0）
1. `transports/ccr_client.py` — 384 行（类 2，函数 0）
1. `transports/hybrid_transport.py` — 388 行（类 1，函数 1）
1. `transports/remote_io.py` — 317 行（类 1，函数 1）
1. `transports/serial_batch_event_uploader.py` — 503 行（类 3，函数 0）
1. `transports/sse_transport.py` — 244 行（类 2，函数 0）
1. `transports/transport_utils.py` — 134 行（类 1，函数 2）
1. `transports/websocket_transport.py` — 961 行（类 2，函数 1）
1. `transports/worker_state_uploader.py` — 160 行（类 2，函数 1）

## 3. 关键符号

1. 文件 `transports/__init__.py`
1. 文件 `transports/ccr_client.py`
1. 类 CCRClientOptions — L64
1. 类 CCRClient — L78
1. 文件 `transports/hybrid_transport.py`
1. 类 HybridTransport — L100
1. 函数 _convert_ws_url_to_post_url — L353
1. 文件 `transports/remote_io.py`
1. 类 RemoteIO — L96
1. 函数 _is_debug_mode — L80
1. 文件 `transports/serial_batch_event_uploader.py`
1. 类 RetryableError — L70
1. 类 SerialBatchEventUploaderConfig — L89
1. 类 SerialBatchEventUploader — L163
1. 文件 `transports/sse_transport.py`
1. 类 SSEEvent — L45
1. 类 SSETransport — L58
1. 文件 `transports/transport_utils.py`
1. 类 Transport — L52
1. 函数 is_env_truthy — L65
1. 函数 get_transport_for_url — L72
1. 文件 `transports/websocket_transport.py`
1. 类 WebSocketTransportOptions — L149
1. 类 WebSocketTransport — L178
1. 函数 _monotonic_ms — L168
1. 文件 `transports/worker_state_uploader.py`
1. 类 WorkerStateUploaderConfig — L46
1. 类 WorkerStateUploader — L64
1. 函数 _coalesce_patches — L137

## 4. 依赖与调用边（import 级）

1. transports/__init__.py → import __future__（外部）
1. transports/__init__.py → import src（外部）
1. transports/ccr_client.py → import __future__（外部）
1. transports/ccr_client.py → import asyncio（外部）
1. transports/ccr_client.py → import logging（外部）
1. transports/ccr_client.py → import random（外部）
1. transports/ccr_client.py → import collections（外部）
1. transports/ccr_client.py → import dataclasses（外部）
1. transports/ccr_client.py → import typing（外部）
1. transports/ccr_client.py → import httpx（外部）
1. transports/ccr_client.py → import src（外部）
1. transports/hybrid_transport.py → import __future__（外部）
1. transports/hybrid_transport.py → import asyncio（外部）
1. transports/hybrid_transport.py → import logging（外部）
1. transports/hybrid_transport.py → import collections（外部）
1. transports/hybrid_transport.py → import typing（外部）
1. transports/hybrid_transport.py → import urllib（外部）
1. transports/hybrid_transport.py → import httpx（外部）
1. transports/hybrid_transport.py → import src（外部）
1. transports/remote_io.py → import __future__（外部）
1. transports/remote_io.py → import asyncio（外部）
1. transports/remote_io.py → import logging（外部）
1. transports/remote_io.py → import os（外部）
1. transports/remote_io.py → import sys（外部）
1. transports/remote_io.py → import collections（外部）
1. transports/remote_io.py → import typing（外部）
1. transports/remote_io.py → import src（外部）
1. transports/serial_batch_event_uploader.py → import __future__（外部）
1. transports/serial_batch_event_uploader.py → import asyncio（外部）
1. transports/serial_batch_event_uploader.py → import json（外部）
1. transports/serial_batch_event_uploader.py → import logging（外部）
1. transports/serial_batch_event_uploader.py → import random（外部）
1. transports/serial_batch_event_uploader.py → import collections（外部）
1. transports/serial_batch_event_uploader.py → import dataclasses（外部）
1. transports/serial_batch_event_uploader.py → import typing（外部）
1. transports/sse_transport.py → import __future__（外部）
1. transports/sse_transport.py → import asyncio（外部）
1. transports/sse_transport.py → import logging（外部）
1. transports/sse_transport.py → import collections（外部）
1. transports/sse_transport.py → import dataclasses（外部）
1. transports/sse_transport.py → import httpx（外部）
1. transports/sse_transport.py → import httpx_sse（外部）
1. transports/sse_transport.py → import src（外部）
1. transports/transport_utils.py → import __future__（外部）
1. transports/transport_utils.py → import os（外部）
1. transports/transport_utils.py → import collections（外部）
1. transports/transport_utils.py → import typing（外部）
1. transports/transport_utils.py → import httpx（外部）
1. transports/transport_utils.py → import src（外部）
1. transports/websocket_transport.py → import __future__（外部）
1. transports/websocket_transport.py → import asyncio（外部）
1. transports/websocket_transport.py → import json（外部）
1. transports/websocket_transport.py → import logging（外部）
1. transports/websocket_transport.py → import random（外部）
1. transports/websocket_transport.py → import time（外部）
1. transports/websocket_transport.py → import collections（外部）
1. transports/websocket_transport.py → import dataclasses（外部）
1. transports/websocket_transport.py → import typing（外部）
1. transports/websocket_transport.py → import websockets（外部）
1. transports/websocket_transport.py → import src（外部）
1. transports/worker_state_uploader.py → import __future__（外部）
1. transports/worker_state_uploader.py → import asyncio（外部）
1. transports/worker_state_uploader.py → import random（外部）
1. transports/worker_state_uploader.py → import collections（外部）
1. transports/worker_state_uploader.py → import dataclasses（外部）
1. transports/worker_state_uploader.py → import typing（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. R7-02 Interfaces — 候选（启发式），状态 UNVERIFIED
1. R5-01 Surface Layer — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → R7-02, R5-01（UNVERIFIED）
1. `ccr_client.py` → R7-02, R5-01（UNVERIFIED）
1. `hybrid_transport.py` → R7-02, R5-01（UNVERIFIED）
1. `remote_io.py` → R7-02, R5-01（UNVERIFIED）
1. `serial_batch_event_uploader.py` → R7-02, R5-01（UNVERIFIED）
1. `sse_transport.py` → R7-02, R5-01（UNVERIFIED）
1. `transport_utils.py` → R7-02, R5-01（UNVERIFIED）
1. `websocket_transport.py` → R7-02, R5-01（UNVERIFIED）
1. `worker_state_uploader.py` → R7-02, R5-01（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
