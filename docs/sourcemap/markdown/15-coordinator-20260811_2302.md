# coordinator 源码盘点
> 编号: 15
> 时间戳: 20260811_2302
> 基线: def709361a86
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 类型: package（目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: coordinator
2. 类型: package（目录模块）
3. Python 文件数: 4
4. 总行数: 794
5. 依赖的 src 内部模块: 无
6. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `coordinator/__init__.py` — 62 行（类 0，函数 0）
1. `coordinator/mode.py` — 303 行（类 0，函数 8）
1. `coordinator/prompt.py` — 369 行（类 0，函数 2）
1. `coordinator/worker_agent.py` — 60 行（类 0，函数 1）

## 3. 关键符号

1. 文件 `coordinator/__init__.py`
1. 文件 `coordinator/mode.py`
1. 函数 is_coordinator_mode — L41
1. 函数 match_session_mode — L55
1. 函数 is_pr_activity_subscription_tool — L134
1. 函数 filter_coordinator_tools — L142
1. 函数 coordinator_main_loop_registry — L156
1. 函数 filter_worker_tools — L185
1. 函数 _build_worker_tools_string — L215
1. 函数 get_coordinator_user_context — L238
1. 文件 `coordinator/prompt.py`
1. 函数 _resolve_worker_capabilities — L90
1. 函数 get_coordinator_system_prompt — L98
1. 文件 `coordinator/worker_agent.py`
1. 函数 get_coordinator_agents — L45

## 4. 依赖与调用边（import 级）

1. coordinator/__init__.py → import annotations（外部）
1. coordinator/__init__.py → import json（外部）
1. coordinator/__init__.py → import Path（外部）
1. coordinator/__init__.py → import INTERNAL_WORKER_TOOLS（外部）
1. coordinator/__init__.py → import coordinator_main_loop_registry（外部）
1. coordinator/__init__.py → import filter_coordinator_tools（外部）
1. coordinator/__init__.py → import filter_worker_tools（外部）
1. coordinator/__init__.py → import get_coordinator_user_context（外部）
1. coordinator/__init__.py → import is_coordinator_mode（外部）
1. coordinator/__init__.py → import match_session_mode（外部）
1. coordinator/__init__.py → import get_coordinator_system_prompt（外部）
1. coordinator/__init__.py → import WORKER_AGENT（外部）
1. coordinator/__init__.py → import get_coordinator_agents（外部）
1. coordinator/mode.py → import annotations（外部）
1. coordinator/mode.py → import logging（外部）
1. coordinator/mode.py → import os（外部）
1. coordinator/mode.py → import Final（外部）
1. coordinator/mode.py → import Iterable（外部）
1. coordinator/mode.py → import Literal（外部）
1. coordinator/mode.py → import TYPE_CHECKING（外部）
1. coordinator/mode.py → import ASYNC_AGENT_ALLOWED_TOOLS（外部）
1. coordinator/mode.py → import is_env_truthy（外部）
1. coordinator/prompt.py → import annotations（外部）
1. coordinator/prompt.py → import is_env_truthy（外部）
1. coordinator/prompt.py → import AGENT_TOOL_NAME（外部）
1. coordinator/worker_agent.py → import annotations（外部）
1. coordinator/worker_agent.py → import replace（外部）
1. coordinator/worker_agent.py → import EXPLORE_AGENT（外部）
1. coordinator/worker_agent.py → import GENERAL_PURPOSE_AGENT（外部）
1. coordinator/worker_agent.py → import PLAN_AGENT（外部）
1. coordinator/worker_agent.py → import AgentDefinition（外部）

## 5. R7/R5/CCR-14 映射（启发式初版）

1. CCR-06 Tool Orchestration — 候选（启发式），状态 UNVERIFIED

## 6. 文件级映射细分

1. `__init__.py` → CCR-06（UNVERIFIED）
1. `mode.py` → CCR-06（UNVERIFIED）
1. `prompt.py` → CCR-06（UNVERIFIED）
1. `worker_agent.py` → CCR-06（UNVERIFIED）

## 7. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ reference 侧对应文件/symbol 映射待核实
1. ☐ 关键 call-edge/state-edge 待逐条对照 reference source
1. ☐ 本模块 R7/R5/CCR 归属的 UNVERIFIED 状态待升级或降级
