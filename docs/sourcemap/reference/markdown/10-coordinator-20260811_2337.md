# coordinator（reference）源码盘点
> 编号: 10
> 时间戳: 20260811_2337
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 来源: restored-src/src/coordinator
> 类型: package（reference 目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: coordinator
2. 类型: package（reference 目录模块）
3. TS/JS 文件数: 1
4. 总行数: 369
5. 依赖的 reference 内部模块: constants, services, tools, utils
6. Python 侧候选映射: coordinator（UNVERIFIED）
7. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `coordinator/coordinatorMode.ts` — 369 行（类 0，函数 5）

## 3. 关键符号

1. 文件 `coordinator/coordinatorMode.ts`
1. 函数 isScratchpadGateEnabled — L25
1. 函数 isCoordinatorMode — L35
1. 函数 matchSessionMode — L49
1. 函数 getCoordinatorUserContext — L79
1. 函数 getCoordinatorSystemPrompt — L110

## 4. 依赖与调用边（import 级）

1. coordinator/coordinatorMode.ts → import constants（内部）
1. coordinator/coordinatorMode.ts → import services（内部）
1. coordinator/coordinatorMode.ts → import tools（内部）
1. coordinator/coordinatorMode.ts → import utils（内部）

## 5. Python 侧候选映射（启发式初版）

1. → python `coordinator` — 候选（启发式），状态 UNVERIFIED

## 6. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ python 候选映射待逐符号对照核实（symbol 级 map 未生成）
1. ☐ 关键 call-edge/state-edge 待逐条对照 python 实现
1. ☐ 顶层符号提取为启发式正则，需人工抽样验证召回率
