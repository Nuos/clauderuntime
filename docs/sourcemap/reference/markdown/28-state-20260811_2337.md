# state（reference）源码盘点
> 编号: 28
> 时间戳: 20260811_2337
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 来源: restored-src/src/state
> 类型: package（reference 目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: state
2. 类型: package（reference 目录模块）
3. TS/JS 文件数: 6
4. 总行数: 1190
5. 依赖的 reference 内部模块: Task, Tool, bootstrap, bridge, commands, context, hooks, services, tasks, tools, types, utils
6. Python 侧候选映射: state（UNVERIFIED）
7. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `state/AppState.tsx` — 199 行（类 0，函数 8）
1. `state/AppStateStore.ts` — 569 行（类 0，函数 2）
1. `state/onChangeAppState.ts` — 171 行（类 0，函数 2）
1. `state/selectors.ts` — 76 行（类 0，函数 2）
1. `state/store.ts` — 34 行（类 0，函数 1）
1. `state/teammateViewHelpers.ts` — 141 行（类 0，函数 5）

## 3. 关键符号

1. 文件 `state/AppState.tsx`
1. 函数 AppStateProvider — L37
1. 函数 _temp — L111
1. 函数 useAppStore — L117
1. 函数 useAppState — L142
1. 函数 useSetAppState — L170
1. 函数 useAppStateStore — L177
1. 函数 useAppStateMaybeOutsideOfProvider — L186
1. 函数 AppStoreContext — L27
1. 文件 `state/AppStateStore.ts`
1. 函数 getDefaultAppState — L455
1. 函数 IDLE_SPECULATION_STATE — L78
1. 文件 `state/onChangeAppState.ts`
1. 函数 externalMetadataToAppState — L24
1. 函数 onChangeAppState — L42
1. 文件 `state/selectors.ts`
1. 函数 getViewedTeammateTask — L18
1. 函数 getActiveAgentForInput — L59
1. 文件 `state/store.ts`
1. 函数 createStore — L9
1. 文件 `state/teammateViewHelpers.ts`
1. 函数 isLocalAgent — L14
1. 函数 release — L28
1. 函数 enterTeammateView — L46
1. 函数 exitTeammateView — L88
1. 函数 stopOrDismissAgent — L116

## 4. 依赖与调用边（import 级）

1. state/AppState.tsx → import context（内部）
1. state/AppState.tsx → import hooks（内部）
1. state/AppState.tsx → import utils（内部）
1. state/AppState.tsx → import state（外部）
1. state/AppStateStore.ts → import context（内部）
1. state/AppStateStore.ts → import utils（内部）
1. state/AppStateStore.ts → import bridge（内部）
1. state/AppStateStore.ts → import commands（内部）
1. state/AppStateStore.ts → import services（内部）
1. state/AppStateStore.ts → import Tool（内部）
1. state/AppStateStore.ts → import tasks（内部）
1. state/AppStateStore.ts → import tools（内部）
1. state/AppStateStore.ts → import types（内部）
1. state/AppStateStore.ts → import state（外部）
1. state/onChangeAppState.ts → import bootstrap（内部）
1. state/onChangeAppState.ts → import utils（内部）
1. state/onChangeAppState.ts → import state（外部）
1. state/selectors.ts → import tasks（内部）
1. state/selectors.ts → import state（外部）
1. state/teammateViewHelpers.ts → import services（内部）
1. state/teammateViewHelpers.ts → import Task（内部）
1. state/teammateViewHelpers.ts → import tasks（内部）
1. state/teammateViewHelpers.ts → import state（外部）

## 5. Python 侧候选映射（启发式初版）

1. → python `state` — 候选（启发式），状态 UNVERIFIED

## 6. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ python 候选映射待逐符号对照核实（symbol 级 map 未生成）
1. ☐ 关键 call-edge/state-edge 待逐条对照 python 实现
1. ☐ 顶层符号提取为启发式正则，需人工抽样验证召回率
