# remote（reference）源码盘点
> 编号: 22
> 时间戳: 20260811_2337
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 来源: restored-src/src/remote
> 类型: package（reference 目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: remote
2. 类型: package（reference 目录模块）
3. TS/JS 文件数: 4
4. 总行数: 1127
5. 依赖的 reference 内部模块: Tool, constants, entrypoints, types, utils
6. Python 侧候选映射: remote（UNVERIFIED）
7. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `remote/RemoteSessionManager.ts` — 343 行（类 1，函数 2）
1. `remote/SessionsWebSocket.ts` — 404 行（类 1，函数 1）
1. `remote/remotePermissionBridge.ts` — 78 行（类 0，函数 2）
1. `remote/sdkMessageAdapter.ts` — 302 行（类 0，函数 11）

## 3. 关键符号

1. 文件 `remote/RemoteSessionManager.ts`
1. 类 RemoteSessionManager — L95
1. 函数 isSDKMessage — L22
1. 函数 createRemoteSessionConfig — L329
1. 文件 `remote/SessionsWebSocket.ts`
1. 类 SessionsWebSocket — L82
1. 函数 isSessionsMessage — L45
1. 文件 `remote/remotePermissionBridge.ts`
1. 函数 createSyntheticAssistantMessage — L12
1. 函数 createToolStub — L53
1. 文件 `remote/sdkMessageAdapter.ts`
1. 函数 convertAssistantMessage — L31
1. 函数 convertStreamEvent — L45
1. 函数 convertResultMessage — L55
1. 函数 convertInitMessage — L74
1. 函数 convertStatusMessage — L88
1. 函数 convertToolProgressMessage — L111
1. 函数 convertCompactBoundaryMessage — L128
1. 函数 convertSDKMessage — L168
1. 函数 isSessionEndMessage — L283
1. 函数 isSuccessResult — L290
1. 函数 getResultText — L297

## 4. 依赖与调用边（import 级）

1. remote/RemoteSessionManager.ts → import entrypoints（内部）
1. remote/RemoteSessionManager.ts → import utils（内部）
1. remote/RemoteSessionManager.ts → import remote（外部）
1. remote/SessionsWebSocket.ts → import constants（内部）
1. remote/SessionsWebSocket.ts → import entrypoints（内部）
1. remote/SessionsWebSocket.ts → import utils（内部）
1. remote/remotePermissionBridge.ts → import entrypoints（内部）
1. remote/remotePermissionBridge.ts → import Tool（内部）
1. remote/remotePermissionBridge.ts → import types（内部）
1. remote/remotePermissionBridge.ts → import utils（内部）
1. remote/sdkMessageAdapter.ts → import entrypoints（内部）
1. remote/sdkMessageAdapter.ts → import types（内部）
1. remote/sdkMessageAdapter.ts → import utils（内部）

## 5. Python 侧候选映射（启发式初版）

1. → python `remote` — 候选（启发式），状态 UNVERIFIED

## 6. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ python 候选映射待逐符号对照核实（symbol 级 map 未生成）
1. ☐ 关键 call-edge/state-edge 待逐条对照 python 实现
1. ☐ 顶层符号提取为启发式正则，需人工抽样验证召回率
