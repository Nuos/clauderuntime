# server（reference）源码盘点
> 编号: 25
> 时间戳: 20260811_2337
> Reference: Claude Code 2.1.88 @ a8a678cb6244
> 来源: restored-src/src/server
> 类型: package（reference 目录模块）
> 盘点状态: INVENTORY_COMPLETE
> 映射状态: UNVERIFIED（启发式初版，待逐项核实）

## 1. 模块概况

1. 模块名: server
2. 类型: package（reference 目录模块）
3. TS/JS 文件数: 3
4. 总行数: 358
5. 依赖的 reference 内部模块: entrypoints, remote, utils
6. Python 侧候选映射: server（UNVERIFIED）
7. 一句话职责: UNKNOWN（待人工核实）

## 2. 文件清单

1. `server/createDirectConnectSession.ts` — 88 行（类 1，函数 1）
1. `server/directConnectManager.ts` — 213 行（类 1，函数 1）
1. `server/types.ts` — 57 行（类 0，函数 1）

## 3. 关键符号

1. 文件 `server/createDirectConnectSession.ts`
1. 类 DirectConnectError — L11
1. 函数 createDirectConnectSession — L26
1. 文件 `server/directConnectManager.ts`
1. 类 DirectConnectSessionManager — L39
1. 函数 isStdoutMessage — L30
1. 文件 `server/types.ts`
1. 函数 connectResponseSchema — L4

## 4. 依赖与调用边（import 级）

1. server/createDirectConnectSession.ts → import utils（内部）
1. server/createDirectConnectSession.ts → import server（外部）
1. server/directConnectManager.ts → import entrypoints（内部）
1. server/directConnectManager.ts → import remote（内部）
1. server/directConnectManager.ts → import utils（内部）
1. server/types.ts → import utils（内部）

## 5. Python 侧候选映射（启发式初版）

1. → python `server` — 候选（启发式），状态 UNVERIFIED

## 6. 未确认项与待核实项

1. ☐ 模块一句话职责待人工核实
1. ☐ python 候选映射待逐符号对照核实（symbol 级 map 未生成）
1. ☐ 关键 call-edge/state-edge 待逐条对照 python 实现
1. ☐ 顶层符号提取为启发式正则，需人工抽样验证召回率
