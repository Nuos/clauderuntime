# 核心修订：分级 Reference 对齐

> **凡是 Claude Code recovered source 能够确定的功能、模块、函数契约、关键控制行为，仍然以 Claude Code 2.1.88 为对齐目标；只有 Reference 具体实现无法确定或恢复不完整的部分，才降低为“核心功能一致 + Python-native 实现 + 差异透明记录”。**

B6 不是“所有模块都只要求功能类似”，而是：

```text
Reference 可确定
→ 必须优先对齐

Reference 部分可确定
→ 已确定部分必须对齐
→ 未确定部分允许功能类似

Reference 无法确定
→ 只要求核心功能/用户目标一致
→ 必须记录不确定性与 Python 实现

Product Extension
→ 明确与 Reference 分离
→ 不得伪装为 Claude Code 原有行为
```

| Reference 证据等级 | 定义 | 开发要求 |
|---|---|---|
| `R1_CONFIRMED` | 源文件、symbol、函数体/关键控制流能够确认 | **必须对齐功能、模块职责、关键行为和已知安全/状态语义** |
| `R2_PARTIALLY_CONFIRMED` | call-site、接口、返回契约或部分逻辑可确认，但实现体不完整 | **已确认部分必须对齐；未知部分允许 Functional Adaptation** |
| `R3_UNKNOWN` | recovered source 无法确认具体实现，只能确认功能名称/高层目的 | **只要求核心功能一致，并明确标记 UNKNOWN_REFERENCE** |
| `R4_PRODUCT_EXTENSION` | ClaudeRuntime 自己新增的功能或增强 | **独立记录，不计作 Claude Code 对齐能力** |


# 模块与函数标记示例

# 示例 1：Agent Loop — 功能相同，async 结构不同

```python
"""主查询循环。

Reference Mapping
-----------------
REF Source: query.ts::queryLoop
REF Behavior: 模型调用、工具执行、结果回填、继续循环。
PY Owner: src/query/query.py::query
PY Behavior: 使用 Python async generator 组织相同的 Agent 交互目标。
Known Differences: Promise/AsyncGenerator、事件装配、helper 边界不同。
Reason: PYTHON_RUNTIME_ADAPTATION
Functional Status: FUNCTIONAL_ADAPTATION
"""
```

# 示例 2：Snip — Reference 不完整

```python
# REF-DIFF:
# REF: query.ts calls snipCompactIfNeeded under HISTORY_SNIP.
# REF-BEHAVIOR: return contract known; function body not fully recovered.
# PY: snip_compact currently returns messages unchanged.
# DIFF: no historical snip is performed in Python.
# WHY: RECOVERED_SOURCE_GAP.
# USER-IMPACT: LOW.
# SAFETY-IMPACT: NONE.
# STATUS: DEFERRED_REFERENCE_DETAIL.
```

# 示例 3：Scheduler — 不同 persistence 架构

```python
# REF-DIFF:
# REF: cronScheduler.ts includes file-backed tasks / watcher / owner lifecycle.
# PY: SessionCronScheduler uses in-session state + snapshot/restore.
# DIFF: no identical file watcher / cross-process owner takeover.
# WHY: PRODUCT_SCOPE_SIMPLIFICATION.
# USER-IMPACT: LOW for current single-session scheduler use cases.
# SAFETY-IMPACT: NONE.
# STATUS: FUNCTIONAL_ADAPTATION.
```

# 示例 4：macOS Sandbox — 不同底层实现可接受

```python
# REF-DIFF:
# REF: sandbox adapter wraps commands through Anthropic sandbox runtime.
# PY: macOS backend uses sandbox-exec / Seatbelt.
# DIFF: configuration schema and runtime implementation differ.
# WHY: OS_PLATFORM_ADAPTATION + PYTHON_ECOSYSTEM_ADAPTATION.
# USER-IMPACT: LOW for supported policy subset.
# SAFETY-IMPACT: MEDIUM; unsupported rules must remain explicit.
# STATUS: LIMITED.
```

# 示例 5：安全增强

```python
# REF-DIFF:
# REF: reference observable behavior does not prove this additional validation.
# PY: rejects symlinked rule files escaping the configured root.
# DIFF: Python is stricter.
# WHY: SAFETY_STRENGTHENING.
# USER-IMPACT: LOW.
# SAFETY-IMPACT: POSITIVE.
# STATUS: FUNCTIONAL_ADAPTATION.
```


# 推荐新增字段示例

```python
# REFERENCE_CERTAINTY: R1_CONFIRMED
# ALIGNMENT_POLICY: MUST_ALIGN
# REF: query.ts::queryLoop
# PY: src/query/query.py::query
# DIFF: Python 使用 async generator，但已知功能/契约必须保持对齐。
```

```python
# REFERENCE_CERTAINTY: R2_PARTIALLY_CONFIRMED
# ALIGNMENT_POLICY: ALIGN_KNOWN_PART
# REF: query.ts 调用 snipCompactIfNeeded；函数体未完整恢复。
# PY: 当前保守 no-op。
# DIFF: 已知 gate/call contract 对齐；未知算法不猜测。
```
